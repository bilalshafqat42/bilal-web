# Project: Bilal Shafqat — freelance website (UAE)

This file gives context for this codebase. Claude Code (or any AI assistant working in this repo) should read this before making changes, so the site build stays consistent with the brief below.

## Who this is for

Bilal is a UAE-based freelancer building his own personal brand website. He positions himself as a full-fledged, one-person digital marketing and product shop, not a narrow specialist. He personally covers:

- Performance development, web and mobile (builds the sites/apps himself)
- Design, web, app, and social media
- Paid campaigns / ads management (Meta, Google, TikTok)
- Automation (workflow / marketing automation)
- Performance marketing (results-driven reporting)

Positioning line: one person doing what usually takes an agency, no handoffs between departments, single point of contact. The site's main goal is to win clients and read as professional, not just look impressive.

## Hard rules (do not deviate without asking Bilal)

- **CTA wording**: always "Book a free consultation." Never "Book your site/free audit" — Bilal specifically rejected "audit" because it reads as SEO-specialist-only, and he doesn't want to be boxed into one service.
  - **Status: done.** Every CTA button across the homepage and /portfolio now reads "Book a free consultation." The Hero's primary (filled) button carries this; "View my work" is the secondary, outline-style button.
- **Portfolio section**: a grid/wall of client company logos, not project screenshot cards. Detailed case studies with real metrics belong on a separate Portfolio/Case Studies page, not the homepage.
  - **Status: structurally done, content pending.** Homepage shows a logo-wall section that links to `/portfolio`, which holds the full detailed write-ups. The logo tiles are still text placeholders — real client logos haven't been supplied yet.
  - **Decision confirmed:** the logo wall stands alone. Do not add a separate "industries worked in" tag row (Real Estate, SaaS, Startups) alongside it — one trust section here, not two.
- **This is a multi-page site**, not a single-page site. Do not collapse everything onto one homepage.
  - **Status: started.** `/` and `/portfolio` exist as separate routes. Services, About, Blog, FAQ, and a dedicated Contact page are still single sections on the homepage, not their own routes yet (see gaps below).

## Sitemap

- Home
- Services (deep breakdown per service: design, development, paid campaigns, automation, performance marketing)
- Portfolio / Case Studies (full write-ups with real numbers; logo wall on the homepage links here) — **built**
- About (personal story / growth narrative, expanded)
- Blog
- FAQ (full list; homepage only shows 4-5 questions)
- Contact / Book a consultation

## Homepage section order (current build)

This reflects what's actually in the code right now, after the CTA-wording and portfolio/logo-wall fixes.

1. Sticky navbar — CTA: "Book a free consultation." Secondary nav: Home / Services / Work / About / Process / Pricing. "Work" links to `/portfolio`, not an in-page anchor.
2. Hero — **replaced 2026-08-08**, see item 24 below for the current live version: a plain black headline section (`id="home"`, 70/30 split heading+CTA) immediately followed by a separate yellow "About Me" panel (`id="about"`, bio + photo) that scroll-expands from 80% to full bleed. Supersedes the single-photo-panel version from items 20-23.
3. Services — "Seven Services, One Point Of Contact," grouped into 3 categories: Marketing & Growth, Website & App Dev, Brand & Content. Intentional, keep the categorized structure.
4. Logo wall — grid of client company tiles, currently text placeholders (e.g. "[Client Logo — Off-Plan Developer]"), links through to `/portfolio` for the full case studies. This is the sole portfolio/trust section on the homepage (no separate industries tag row, see decision above).
5. About Me — now merged into the Hero's yellow panel (item 24), not a separate homepage section. The standalone `About.tsx` component still exists in the codebase but is no longer rendered on `/`.
6. How I Work — 4-step process: Understand the Brief, Plan & Design, Build & Launch, Optimize & Scale.
7. Growth Partnerships — segments by client type: Startups & Founders, Real Estate Developers & Agencies (UAE), In-House Teams & Growing Companies, Agencies & Consulting Partners.
8. Engagement Models — 4 packages: Project-Based, Monthly Retainer, Ongoing Partner / Dedicated Support, **Consulting & Advisory** (renamed from "Consulting & Audit" — see resolved decisions below).
9. Measurable Outcomes — 4 stat tiles plus 2 testimonial cards in one section. Testimonials are still placeholder content (see gaps).
10. Final CTA — "Book a free consultation," repeated.
11. Footer — contact details, secondary nav (Work now points to `/portfolio`).

### /portfolio (new page)

Full case-study write-ups (goal, role, execution bullets, outcome) for 5 projects: off-plan lead generation, a real estate launch campaign, continuous lead gen for agencies, a custom MERN marketing application, and a mobile app/brand/social launch. The last two still carry bracket placeholders (`[Client Industry]`, `[Add the real business goal]`) pending real client details.

## Resolved decisions (apply these in code)

1. **Package name**: rename "Consulting & Audit" to **"Consulting & Advisory"** everywhere it appears (Engagement Models section, and any pricing/portfolio references). Reason: reusing "audit" conflicts with the CTA-wording rule above, even though it's a package name and not a call-to-action.
2. **Logo wall vs. industries tags**: keep the logo wall only. Do not add a separate "industries worked in" tag row. If a future need arises to show sector coverage, fold it into the About Me or Services copy instead of adding a new homepage section.

## Open gaps (still need to be added/fixed)

- **Testimonials with real names and photos**: still placeholder brackets (`[Client Name]`) in the Measurable Outcomes section. This was flagged as the strongest trust signal after a personal photo.
- **Blog carousel**: not present. Add near the bottom of the homepage, above a future FAQ section.
- **FAQ section**: not present anywhere. Add before the final CTA, capped at 4-5 questions on the homepage (full list to live on a dedicated FAQ page).
- **Real client logos**: the logo wall exists and links to `/portfolio`, but all 5 tiles are text placeholders. Needs actual logo files (with each client's permission) before it reads as a genuine trust signal.
- **Case study placeholder content**: 2 of 5 write-ups on `/portfolio` still have bracket placeholders for goal/outcome text.
- **Remaining sitemap pages**: Services, About, Blog, and FAQ are still homepage sections only, not their own routes. Contact is a homepage section reused as a block on `/portfolio`, not a standalone page either.
- **Lead capture → CRM integration**: the popup form (`LeadFormPopup.tsx`) now posts to `src/app/api/lead/route.ts`, which is built and tested but has nowhere to send leads yet. Needs from Bilal: the CRM's endpoint URL, an API key/token if the endpoint requires auth, and confirmation of the field names it expects (defaults are `name`, `email`, `phone`, `message`, `source`). Once supplied, set `CRM_LEAD_API_URL` / `CRM_LEAD_API_KEY` in `.env.local` — no further code changes needed unless the field names differ.

## Suggestions queue — professionalism & clarity for UAE + global clients (not yet actioned, review on request)

Bilal asked for a running list of ideas to make the site read more professional and easier to understand for clients both inside the UAE and abroad. None of these are built yet — surface this list when he asks what's queued up.

1. **FAQ section, written for cross-border logistics** (extends the existing FAQ gap above): cover payment/currency (AED for UAE clients, USD wire/Stripe for international), time zone expectations, response times, and how contracts/NDAs work for fully remote clients. This is the single highest-leverage item for reducing "will this actually work for me" hesitation from overseas clients.
2. **WhatsApp click-to-chat button**: WhatsApp is the default business channel in the UAE. A lightweight WhatsApp icon alongside (not replacing) the existing lead-form popup would meet local clients on their preferred channel without adding a competing CTA style.
3. **Time zone transparency line**: a short, visible note near the Hero or Contact section — e.g. "Based in Dubai (GST, UTC+4) — typical response within a business day" — reassures both UAE clients of local presence and international clients about when to expect a reply.
4. **Dual-currency pricing signals**: once real package pricing exists, show an indicative starting price in AED with a USD equivalent alongside it in Engagement Models, so visitors from either region immediately gauge budget fit without emailing first.
5. **Plain-language subtitles on technical services**: a one-line, jargon-free translation under technical service names (e.g. "MERN Stack Development" → "custom web apps built around your exact workflow") so non-technical decision-makers, common among international SMB clients, understand the value instantly.
6. **A short Privacy Policy page**: now that the site actively collects lead data into a CRM, a plain-language note on what's collected and why is a baseline professionalism signal for both UAE (PDPL-aware) and international (GDPR-aware) visitors.
7. **Regions-served line**: a simple statement like "Working with clients across the UAE, GCC, Europe, and North America" (only if true) — reassures international visitors he isn't purely local, and reassures UAE clients of his local grounding.
8. **Trust/partner badges**, if Bilal holds any (e.g. Meta Business Partner, Google Partner) — a fast credibility signal for clients comparing a solo freelancer against an agency.
9. **Real testimonials** (already tracked above as a gap) — flagged again here specifically as the top trust signal for international clients who can't easily verify a solo operator's credibility otherwise.
10. **Arabic-language version, longer-term**: worth considering given the GCC client base, but this is a bigger scope item (proper professional translation, RTL layout support) — not a quick add, only pursue once the English site and core content gaps are settled.

### New items added 2026-08-01

11. **Footer social icons** — **done.** Trimmed from 9 placeholder platforms down to the 4 Bilal actually confirmed: LinkedIn, Instagram, Facebook, Behance. Replaced the inconsistent text abbreviations with real icon glyphs (inline SVG for LinkedIn/Instagram/Facebook, a clean "Be" monogram for Behance — no new dependency). All 4 still link to `#` placeholders; still need Bilal's real profile URLs to swap in.
12. **AI assistant chat widget** — queued, not yet built. Auto-opens once per session after the visitor scrolls past the end of the 2nd homepage section (Services). Collects name, email, phone, service of interest, and a meeting time, then sends the data to the CRM (same pipeline as the lead popup, `/api/lead`) and pushes an immediate WhatsApp notification to Bilal at +971 52 976 6006 so he can follow up personally. Decisions made so far:
    - **Meeting time field: confirmed — simple datetime-local input** (not a Cal.com/Calendly embed). Captures the visitor's preferred slot in their own browser timezone; Bilal reconciles manually. Faster to ship, timezone conversion is on Bilal to handle for now.
    - **WhatsApp delivery to Bilal: confirmed — CallMeBot fast path.** Bilal needs to message CallMeBot's activation number once to get a personal API key (free, no business verification) before this can be wired up.
    - **Still open**: the widget's opening framing/copy — recommended line is "Hi, I'm here on Bilal's behalf — want me to grab a few details so he can reach out personally?" so it's honest that a real person follows up rather than the bot pretending to be one. Confirm or adjust this copy before build.
    - Needs from Bilal before build: the CallMeBot API key (once generated), and a yes/adjust on the opening copy above.
13. **WhatsApp click-to-chat button** — **done, then upgraded to a lead form (2026-08-02).** Originally a direct `wa.me` link; Bilal asked to collect fields first and send the data two ways. Now: clicking the floating green button opens a form (name, email, phone, service dropdown, preferred meeting time — a plain datetime-local input), and on submit it does two things at once — opens WhatsApp (`wa.me`) with all the details pre-filled as a message to +971 52 976 6006 ready for the visitor to hit send, and separately posts the same data to `/api/lead` (source: `whatsapp-popup`) as a best-effort CRM copy that doesn't block or affect the WhatsApp flow if the CRM isn't configured yet. No CallMeBot or WhatsApp Business API needed for this — delivery is via the visitor's own WhatsApp app/web, same mechanism as the original button, just with real data instead of a canned line. Tested end to end (headless browser): form opens, WhatsApp deep link carries all 5 fields correctly formatted, CRM POST fires in parallel without blocking the WhatsApp tab. This substantially overlaps with item 12's field list (name/email/phone/service/meeting time) — item 12's distinguishing features that remain open are the **auto-open-on-scroll trigger** and a **silent CallMeBot-based notification to Bilal** (vs. this flow, which is visitor-initiated and requires them to hit send in WhatsApp themselves).

14. **WhatsApp popup redesigned as a step-by-step chat (2026-08-03)** — **done.** Bilal asked for the static form to feel like chatting with a real person instead. `WhatsAppButton.tsx` now runs a proper conversational flow: one question at a time in a chat transcript ("Bilal's Assistant" header, bot bubbles left/muted, user answers right/green), a short typing-indicator pause between questions, and the service step renders as tap-to-answer chips instead of a dropdown. The opening line is deliberately honest — "I'm here on Bilal's behalf so he can personally follow up with you" — rather than having the bot imply it's Bilal himself typing live. On the final answer it does the same dual-send as before (opens the WhatsApp deep link with all 5 answers formatted, and posts to `/api/lead` with source `whatsapp-chat`), then ends with a friendly closing bot message and a Close button.
15. **Widget redocked + proactive auto-open (2026-08-03)** — **done.** Bilal flagged that the centered modal felt like an interrupting popup rather than a real chat widget, and asked for it to auto-open like a sales rep greeting a visitor. Changes: the panel is now docked bottom-right (`bottom-40`, above the launcher and the "Quick Enquiry" button, no overlap), no dark backdrop, and the page stays fully visible and scrollable behind it — confirmed the visitor can still scroll the site while the chat stays open. The launcher button doubles as open/close (icon swaps to an X, plus a small unread dot before first interaction). It also now opens itself once per browser session after the visitor scrolls ~700px down, leading with a warm two-part greeting ("Hi there! Thanks for stopping by — how can I help you today?" → then the name question) rather than jumping straight to data collection — mimicking a rep stepping in rather than a static widget. Manual clicks on the launcher skip the extra greeting line and go straight to the name question, since clicking already signals intent. Tested end to end: confirmed closed on load, confirmed it opens itself with both greeting messages after scrolling past the threshold, confirmed the page remains scrollable/interactive while the panel is open. This closes out the auto-open-on-scroll piece of the old "AI assistant chat widget" idea (item 12) — the only remaining distinct piece of that idea is a silent CallMeBot push straight to Bilal's phone (this flow still requires the visitor to hit send in WhatsApp themselves).
16. **Meeting-time step removed + trigger fixed + no-gatekeeping escape hatch (2026-08-03)** — **done.**
    - Dropped the "preferred meeting time" question entirely. The flow is now 4 steps: name, email, phone, service.
    - Auto-open trigger fixed to match what Bilal asked for: whichever happens first — the visitor scrolling past the Services section (checked via `#services`' position, falls back to a 700px scroll distance on pages without that section, e.g. `/portfolio`) — or 30 seconds elapsed on the page.
    - Bilal flagged that requiring the full Q&A before reaching WhatsApp could feel like a gate blocking access to "a real person." Added a small "Prefer to skip ahead and message Bilal directly?" link, visible under the input at every step from the very first question onward. Clicking it immediately opens WhatsApp with whatever partial details were given so far (or just the generic opener if none), so no one is forced through the full flow to reach a human.
    - Tested end to end: confirmed the flow ends after the service step with no meeting-time question, confirmed the skip-ahead link opens WhatsApp correctly even with zero fields answered.
17. **Services section rebuilt as a numbered hover-list with crossfade preview (2026-08-03)** — **done.** Reference: tentwenty.com's services section (verified live — a hover-driven numbered list with a single preview image that crossfades per row). Also checked landonorris.com per Bilal's request: it runs bespoke WebGL/canvas background art and a scrapbook photo masonry, which doesn't fit this site's B2B tone or scope, so nothing from it was adopted beyond confirming the idea of a section-to-section background color shift (not yet built, low priority). `Services.tsx` now shows a numbered list (01–07, grouped under the existing Marketing & Growth / Product & Engineering / Brand & Content headers) on the right, and a sticky preview panel on the left that crossfades (Framer Motion `AnimatePresence`) between each service's icon, description, and bullet chips on hover or tap — tap works for mobile since there's no hover there. No content was dropped; the same descriptions/bullets from the old card grid now live in the preview panel. Verified live: hovering different rows correctly swaps the panel's accent color, icon, and copy, confirmed on both a Marketing & Growth item and a Brand & Content item.
18. **Animated glow blobs + shrinking nav on scroll (2026-08-03)** — **done.** Reference: a SocialLift template screenshot Bilal shared showing a large glowing purple radial blob behind the CTA button, plus a request for a full-width header that narrows once scrolled.
    - The shared `.blob` class (used in Hero and Contact) now drifts and pulses via a `blob-float` CSS keyframe (14s loop, respects `prefers-reduced-motion`) instead of sitting static. Existing blobs got staggered `animationDelay` values so they don't pulse in sync. Added one larger, more saturated violet blob centered behind Contact's CTA card specifically, to more directly echo the reference image.
    - `Nav.tsx`: at the top of the page the bar is now full-width and flat (no pill background); past the existing 12px scroll threshold it animates down to the previous centered `max-w-7xl` glass pill. Verified via bounding box measurement: 1376px wide at top vs 1232px wide scrolled, on a 1440px viewport — confirmed the shrink is real, not just visual.
    - Honest caveat: the glow reads more subdued than the reference because it sits behind Contact's dark glass card (`glass-strong`, translucent), whereas the reference has no card in front of it. If Bilal wants it more vivid/dominant, it needs either a higher blob opacity or a less opaque card background — flagged, not changed without asking.
19. **Readability pass on Engagement + WhoIWorkWith (2026-08-04)** — **done.** Bilal shared a minimalist reference site ("Viral") and initially asked for a full rebrand to its light/cream color scheme; after a comparison table walked through what that would actually mean (every component, brand identity, the glow/glass effects all rebuilt), he clarified he wants to **keep the current dark/gold colors** — the real complaint was that the reference's cards read more clearly than his own, purely from spacing and content density, not color.
    - Root cause identified concretely (screenshotted both before making any change): the Engagement and WhoIWorkWith cards stacked icon → title → description → a labelled bullet list (or two, in Engagement's case) → an italic summary line — 4 layers of content per card, 4 cards wide. The reference's cards show icon → title → one line, nothing else.
    - Fix: kept every word of existing content (none of it was cut), but moved the detailed bullet lists behind a "See details" toggle per card (Framer Motion height animation) so the default view only shows icon, title, description, and the italic "best for" line. Confirmed live: both sections now fit far more comfortably in one view, and expand/collapse animates smoothly with no console errors.
    - Scope stayed limited to these two sections since they were the concretely identified offenders — Hero, Services, and other sections were checked and are not similarly dense.
20. **Hero replaced with an Anthropic-inspired layout, now live on the homepage (2026-08-06)** — **done.** Bilal referenced anthropic.com's homepage specifically; confirmed live (GSAP + ScrollTrigger genuinely active, 3 instances) before building anything, rather than guessing from memory. Adopted the general technique and layout pattern, not their copy or colors — this is `HeroAlt.tsx` (previously piloted at `/preview-hero-alt`, now swapped in as the live Hero on `page.tsx`, replacing the old `Hero.tsx`).
    - Top area: heading on the left (Bilal's existing headline, restyled — large bold 64px-equivalent type, underline emphasis on key phrases instead of the gold text-gradient) with supporting paragraph + both CTAs ("Book a free consultation" primary, "View my work" secondary) on the right, matching Anthropic's split-heading-and-content structure and type scale.
    - Below: the photo panel starts at 90% width with rounded corners and genuinely animates to a full-bleed 100% square-edged panel as the visitor scrolls, driven by real GSAP `ScrollTrigger` (`gsap` added as a new dependency) — verified by measuring the panel's actual pixel width at multiple scroll positions (not just visually), confirmed it animates smoothly from ~90% to exactly 100% and holds once past the trigger range.
    - Fixed two real bugs surfaced while making this live rather than just a preview: the section was missing `id="home"` (nav's Home link and logo click depend on it) and was missing the secondary "View my work" CTA entirely (only had one button) — both restored before going live. Also fixed the heading initially rendering underneath the fixed nav bar (insufficient top padding).
    - Verified end to end on the actual homepage (not just the preview route): both CTAs present with correct hrefs (`#contact`, `/portfolio`), nav's Home link scrolls correctly, scroll animation reaches a clean full 1440px on a 1440px viewport, mobile layout stacks cleanly, zero console errors.
    - **Content dropped in this version, flagged not silently lost**: the scrolling skills ticker and the inline "15+ Years / 6 Services / Dubai, UAE" stat row that the old Hero had are not present here — the stats now only appear as overlay tags on the photo panel. Add back if Bilal wants that content restored.
    - `/preview-hero-alt` route still exists (compares old `Hero.tsx` against this design) but is now comparing against what's no longer live — low priority to clean up or repurpose.
21. **Hero copy rewrite, logo/favicon wired in, node-network hero visual, nav border bug, and site-wide SEO/GEO build-out (2026-08-07)** — **done.**
    - Headline changed from "One partner for everything from paid ads to shipped product" to "15 years of marketing, design, and development experience — in one senior partner" — Bilal felt the old phrasing read like a dropshipping/ecommerce-hustle pitch rather than a senior consultant. New supporting paragraph emphasizes working directly with clients, no account managers or junior staff.
    - Real logo wired in (`public/logo/bs-logo.svg` wordmark in Nav and Footer, `public/logo/bs.svg` icon mark as the actual favicon via Next's `icon.svg` file convention — had to delete the old default `favicon.ico`, since Next prioritized that file-based convention over the `metadata.icons` setting alone).
    - Nav border bug fully root-caused, not just patched: it wasn't a static leftover border, it was `.glass-strong`'s own 1px border briefly reasserting itself mid-transition while scrolling back up, since the first fix only zeroed the border in the settled "scrolled" state. Fixed by pinning `border-width: 0` unconditionally regardless of scroll state; verified by sampling computed border every 50ms through the full reverse-scroll transition, confirmed 0px at every sample.
    - Hero's photo replaced with an animated "connected service nodes" visual per Bilal's request (inspired by a heynesh.com screenshot he shared, rebuilt from scratch with Bilal's own service names/icons, not their actual code, logo, or testimonials) — 6 floating cards (Paid Social Media Marketing, Performance Marketing, App Development, App Design, Web Development, Social Media Management), each bobbing independently via a new `.node-float` CSS keyframe, joined by thin SVG lines into a loose network shape over the existing grid/glow background.
    - Site-wide SEO/GEO audit done by actually searching the codebase, not assuming: confirmed home + `/portfolio` had metadata, photos had real alt text, but there was **no robots.txt, no sitemap, no llms.txt, and zero structured data anywhere**. Built all four: `public/robots.txt` (explicitly allows GPTBot, ClaudeBot, Google-Extended, PerplexityBot, CCBot), `src/app/sitemap.ts` (auto-generates `/sitemap.xml`), `public/llms.txt` (plain-text site summary for AI crawlers), and `StructuredData.tsx` (Schema.org `Person` + `Service` JSON-LD, using `Person` rather than `Organization` since Bilal operates under his own name, not a separate company brand — includes his real Behance URL as `sameAs`, no fake reviews/testimonials since none exist yet). Added `metadataBase` and per-page canonical URLs. Verified all of it actually renders (curled robots.txt/llms.txt/sitemap.xml, grepped the live HTML for the canonical tag and full JSON-LD payload).
    - **Needs confirmation from Bilal**: all of the above assumes the production domain is `bilalshafqat.com` (from his Behance profile) — if the real domain differs, it only needs correcting in `layout.tsx` (`metadataBase`), `sitemap.ts`, `robots.txt`, `llms.txt`, and `StructuredData.tsx`.
    - **Still open, not built**: `FAQPage` and `Review` JSON-LD schema — deliberately skipped since there's no real FAQ content or real testimonials yet; adding fake review schema would be actively harmful (Google penalizes fabricated review markup), so this should only be built once item 9's placeholder testimonials are replaced with real ones.
    - Bilal also asked about a small text element appearing above/before the hero section, referencing anthropic.com — checked anthropic.com again and did not find such an element on their actual current homepage (nav flows straight into the hero). Flagged back to Bilal to clarify what he's referring to rather than guessing.
22. **GSAP load-in entrance sequence + photo brought back with a giant wordmark (2026-08-07)** — **done, hero section only, nothing else touched per Bilal's explicit instruction.** Bilal studied a reference site frame-by-frame (heynesh.com, shared earlier) and had another Claude session turn that study into a precise technical spec, which was implemented faithfully with Bilal's own name, photo, and colors, not the reference's actual code, logo, or copy.
    - Brought the photo back (it had been swapped for the floating-node visual two sessions ago) and added a giant decorative "BILAL" wordmark in gold behind it, `aria-hidden="true"` so the real `<h1>` stays the actual heading for accessibility/SEO. Sized via `clamp()` so it genuinely extends past the photo's edges on both sides — first attempt (`clamp(4rem, 18vw, 14rem)`) sized it almost exactly as wide as the photo (626px vs. the photo's ~630px), completely hiding it; caught this by measuring actual rendered bounding boxes rather than trusting the screenshot alone, then sized up to `clamp(6rem, 32vw, 30rem)` and confirmed the letters now visibly peek out on both sides.
    - Built the full sequence as a single `gsap.timeline()` via `@gsap/react`'s `useGSAP` hook (new dependency): wordmark scales/fades in first, then the photo focus-in (blur 30px → 0, scale 0.94 → 1), then headline, paragraph, stat badges, and CTAs cascade in with staggered `"<0.1"`-style position params, each blurred+offset until its turn. Gated on the photo's `onLoad` firing (not `onLoadingComplete`, which is deprecated in this Next.js version) so the timeline can't start before the asset is actually visible.
    - Respects `prefers-reduced-motion`: confirmed via Playwright with `reducedMotion: "reduce"` that the headline group's opacity is already `1` at 300ms in, far before the ~2s animation would normally finish, meaning it skips straight to final state rather than animating.
    - The pre-existing scroll-driven width expansion (90% → 100% panel) from item 20 is untouched and still works: confirmed the panel measures ~1350px settled, then genuinely reaches a clean 1440px (full width) after scrolling, same mechanism as before, now coexisting with the new load-in timeline via a second, independent `useGSAP` call.
    - Real content stays present in the DOM at first paint — only opacity/filter/transform are animated, nothing is hidden via CSS classes — so this shouldn't hurt LCP or hide content from crawlers/no-JS visitors, per Bilal's own spec requirement.
23. **Wordmark removed, reverted to the single-photo composition (2026-08-07)** — **done.** Bilal saw item 22 live and said the hero "doesn't look good." Rather than guess at a fix, gave an honest diagnosis: the giant wordmark is a bold "creative portfolio" flex that fits a flashy Webflow-developer personal brand, but fights against Bilal's actual positioning ("15 years, senior, no-nonsense partner") — the loud gold lettering competes with the headline instead of reinforcing credibility. Recommended dropping the wordmark and returning to the calmer single-photo-with-floating-stat-card composition Bilal had responded well to earlier, while keeping the new GSAP entrance choreography from item 22. Bilal confirmed. Removed the wordmark entirely (and its now-unused ref), restored the photo to full-bleed `object-cover` within the panel, restored the "15+ years" floating stat card top-right and the Dubai/6-Services tags bottom-left. Entrance timeline simplified to match: photo focus-in is now step 1 (no wordmark step before it), headline/paragraph/stats/CTA cascade in after, same stagger technique as before. Confirmed live: clean composition, no console errors, entrance animation settles correctly.
24. **Split black-headline + yellow-About-box hero, now live on the homepage (2026-08-08)** — **done.** Bilal pivoted the hero to two visually separate blocks: a plain black top section keeping the existing 70/30 headline+CTA split (`id="home"`, untouched from before), and a standalone yellow "About Me" panel below it (`id="about"`) with the bio content and photo, that starts at 80% width/height and scroll-expands to full bleed via GSAP `ScrollTrigger` — piloted first at `/preview-hero-yellow` (`HeroYellowTest.tsx`) before going live, per the established pattern of testing riskier layout changes in isolation first.
    - **Width fix**: the yellow panel's initial width now matches the black headline's container exactly, rather than an arbitrary 80% of viewport. Changed from inline `style={{ width: "80%" }}` to `className="mx-auto max-w-7xl"` (same container class the heading uses), removing the width override from the GSAP tween. Verified by measuring both containers on the live page: heading container 1280px, yellow panel 1280px, exact match.
    - **Typography upgrade sitewide**: bumped the shared `SectionHeading.tsx` component (used by Services, LogoWall, Process, WhoIWorkWith, Engagement, Results) from `text-3xl sm:text-4xl` to `text-4xl sm:text-5xl` for H2s, with the description text and max-width container sized up to match — this single change propagates larger, more confident headings across every major homepage section. The hero's own H1 was sized up to `text-6xl sm:text-7xl lg:text-[4.5rem]`, and the yellow panel's H2 to `text-3xl sm:text-4xl lg:text-5xl` — following standard type-scale conventions (roughly 1.25–1.33x steps between heading levels) rather than arbitrary sizes.
    - **Went live**: copied the finalized preview component into `HeroAlt.tsx` (the actual live hero, replacing the previous single-photo-panel version from items 20-23) and removed the now-redundant standalone `About` section from `page.tsx`, since its photo+bio content now lives inside the yellow panel. `About.tsx` itself was left in place, unused, not deleted. Preserved `id="home"` and `id="about"` on the two new sections so Nav's existing anchor links keep working without changes.
    - Verified end to end on the live homepage (not just the preview route): `npm run build` succeeded cleanly; confirmed via Playwright that both section IDs exist, the width match holds (1280px = 1280px), and there are zero console/page errors.
    - `/preview-hero-yellow` and the earlier `/preview-hero-wordmark` routes still exist but are now stale/superseded — low priority to clean up, consistent with not deleting exploratory work that might be revisited.
25. **Modern type scale (h1 56px / h2 44px) sitewide, and a real bug fix to the scroll-grow effect (2026-08-10)** — **done.** Bilal asked for exact desktop sizes matching Anthropic's own scale (h1 56px, h2 44px) rather than the larger 72px/48px sizes from item 24 — that item had overshot "bigger" into oversized territory.
    - Set H1 (hero headline, `HeroAlt.tsx` + `HeroYellowTest.tsx`) to `text-4xl sm:text-5xl lg:text-[3.5rem]` (56px desktop). Set every true H2 sitewide (`SectionHeading.tsx`, plus `Results.tsx` and `Contact.tsx`, which don't use the shared component, plus the yellow panel's own H2) to `lg:text-[2.75rem]` (44px desktop), each with a proportional mobile/tablet ramp underneath. H3s got a smaller proportional bump: card-title H3s in `Services.tsx` and `CaseStudies.tsx` moved toward a ~32px step, and the compact 4-column card titles in `Process.tsx`, `WhoIWorkWith.tsx`, `Engagement.tsx` went from a flat 18px to 18px/20px responsive, deliberately kept smaller than a full H3 jump since bumping them to 32px would have broken those tight grid layouts. Verified all sizes via computed-style measurement on the live page, not just visually.
    - **Real bug found and fixed**: the yellow panel's "grows to full-bleed on scroll" animation (originally from item 20, carried into item 24) had been silently broken by item 24's own width-match fix — adding the `max-w-7xl` class to match the heading's width also permanently capped the panel's max-width, so the GSAP tween's `width: "100%"` target could never exceed 1280px. Measured directly: panel width was 1280px before, during, and after scrolling — no growth at all. Fixed by dropping the `max-w-7xl` class in favor of an inline `width: "min(80rem, 100%)"` (mathematically identical at load, but not a hard cap), so the same tween can now genuinely grow past it.
    - **Second bug found while fixing the first**: once width could move again, measurement showed the panel wasn't starting at a clean 0%, either. It sits close enough to the top of the page that the element-relative trigger (`top 90%` / `top 10%`) was already ~28% into its range at load (1334px instead of 1280px), because there wasn't enough room above it for that viewport-relative math to resolve to zero. Fixed by switching the trigger to the same page-scroll-distance pattern used earlier in this project for an equivalent bug: `trigger: heroRef.current, start: "top top", end: "+=600"` — this ties progress to actual scroll distance instead of the panel's position relative to the viewport, guaranteeing a clean 0% at load regardless of hero height.
    - Verified end to end: `npm run build` passes clean; measured on the live page at a 1440x900 viewport — initial state 1280px (exact match with the heading), scrolled state 1440x900 (full bleed, square corners); zero console errors; screenshots confirm the visual result at both states.

Reference sites (adapt style, do not copy content):
- https://www.brionycullin.com/ (low-friction consultation CTA)
- https://www.punith.com/ (process steps)
- https://muradmurad.com/ (industries worked in — style reference only; not used as a separate homepage section, see resolved decisions)
- https://www.ryrob.com/ (growth story section — currently folded into About Me, could be pulled back out as its own section if it gets lost)
- https://www.sajidsulaiman.com/ (detailed breakdown section — currently folded into About Me)
- https://growwithward.com/ ("how I grow your business" framing — currently expressed via Growth Partnerships/Engagement Models instead)

## UX target

- Core Web Vitals (2026 Google real-user thresholds): LCP under 2.0s, INP under 200ms, CLS under 0.1
- WCAG AA accessibility: contrast, alt text on every image/logo, full keyboard navigation
- A real photo or short video of Bilal near the hero/process section (strongest personal trust signal for a solo freelancer) — **done**, keep it in the hero
- On mobile, collapse the deepest sections (growth story, detailed breakdown, "how I grow your business") or move that depth to inner pages so the homepage stays skimmable
- One consistent CTA style repeated throughout, never introduce a second competing CTA style
- Real user testing (heatmaps/session recordings) after launch — this is what actually closes the gap to excellent UX, not layout alone

## SEO target (Google, Bing — Yahoo results are Bing-powered so this covers both)

- HTTPS, XML sitemap submitted to Google Search Console and Bing Webmaster Tools
- Canonical URLs, unique meta title/description per page
- JSON-LD structured data: Organization, Service, FAQPage, Review schema
- Alt text on all images including the logo wall
- Internal linking across homepage, services, portfolio, blog
- E-E-A-T content signals: real case studies, credentials, named testimonials, blog content answering real client search queries
- Backlinks from credible sites (slow-moving factor, needs ongoing outreach, not a one-time build task)

## AI search / GEO target (ChatGPT, Claude, Perplexity, Gemini, Copilot)

This is an evolving space with no official published ranking algorithm from these providers as of 2026. Revisit every few months.

- robots.txt: explicitly allow GPTBot, ClaudeBot, Google-Extended, PerplexityBot; decide CCBot case by case
- Add an llms.txt file describing site content and sections. Note: as of early 2026, roughly 90% of AI crawler traffic ignores llms.txt and reads HTML directly, so clean semantic HTML matters more than the file itself
- Structure key content in question-first, FAQ-style blocks that match how clients actually phrase search prompts
- Use specific numbers in case studies (e.g. "3.2x ROAS") since AI engines favor evidence-dense, extractable content over vague claims
- Keep content refreshed; AI citation pools favor recently updated pages over stale ones

## Important caveat to keep repeating to Bilal

No on-page work guarantees a top ranking on Google, Bing, Yahoo, or citation in AI search results. Backlinks, domain age, and competition all matter and take months to build. This file describes what's fully controllable through design and technical build quality, not a ranking guarantee.