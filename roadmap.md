# Project: Bilal Shafqat — freelance website (UAE)

This file gives context for this codebase. Claude Code (or any AI assistant working in this repo) should read this before making changes, so the site build stays consistent with the brief below.

## Who this is for

Bilal is a UAE-based freelancer building his own personal brand website. He positions himself as a full-fledged, one-person digital marketing and product shop, not a narrow specialist. He personally covers:

- Performance development, web and mobile (builds the sites/apps himself)
- Design, web, app, and social media
- Paid campaigns / ads management (Meta, Google, TikTok)
- Automation (workflow / marketing automation)
- Performance marketing (results-driven reporting)

Positioning line: one person doing what usually takes an agency, no handoffs between departments, single point of contact. The site's main goal is to win clients and read as professional, not just look impressive.

## Hard rules (do not deviate without asking Bilal)

- **CTA wording**: always "Book a free consultation." Never "Book your site/free audit" — Bilal specifically rejected "audit" because it reads as SEO-specialist-only, and he doesn't want to be boxed into one service.
  - **Status: done.** Every CTA button across the homepage and /portfolio now reads "Book a free consultation." The Hero's primary (filled) button carries this; "View my work" is the secondary, outline-style button.
  - **Amended 2026-08-26, on Bilal's explicit instruction.** The ban applies to the **site-wide CTA**, which stays "Book a free consultation" everywhere. It does **not** ban "audit" as the name of a productised service on its own page (SEO audit, campaign audit, accessibility audit). The original concern was being boxed in as an SEO specialist by the primary call-to-action; a page selling a defined, priced deliverable does not carry that risk. See Phase 3 item 15.
- **Portfolio section**: a grid/wall of client company logos, not project screenshot cards. Detailed case studies with real metrics belong on a separate Portfolio/Case Studies page, not the homepage.
  - **Status: structurally done, content pending.** Homepage shows a logo-wall section that links to `/portfolio`, which holds the full detailed write-ups. The logo tiles are still text placeholders — real client logos haven't been supplied yet.
  - **Decision confirmed:** the logo wall stands alone. Do not add a separate "industries worked in" tag row (Real Estate, SaaS, Startups) alongside it — one trust section here, not two.
- **This is a multi-page site**, not a single-page site. Do not collapse everything onto one homepage.
  - **Status: mostly done.** Real routes now exist for `/`, `/services` (+ 4 pillar pages), `/portfolio`, `/about`, and `/contact`. Only **Blog** and **FAQ** remain homepage-section-or-nothing (see Phase 3 of the forward roadmap).

## Sitemap

- Home
- Services (deep breakdown per service: design, development, paid campaigns, automation, performance marketing) — **built**, as `/services` plus 4 pillar pages
- Portfolio / Case Studies (full write-ups with real numbers; logo wall on the homepage links here) — **built**
- About (personal story / growth narrative, expanded) — **built** 2026-08-18, see item 42
- Blog — **not built** (Phase 3, deliberately last)
- FAQ (full list; homepage only shows 4-5 questions) — **not built** (Phase 3)
- Contact / Book a consultation — **built** 2026-08-18, see item 42

## Homepage section order (current build)

This reflects what's actually in the code right now, after the CTA-wording and portfolio/logo-wall fixes.

1. Sticky navbar — CTA: "Book a free consultation." Secondary nav: Home / Services / Work / About / Process / Pricing. "Work" links to `/portfolio`, not an in-page anchor.
2. Hero — **replaced 2026-08-08**, see item 24 below for the current live version: a plain black headline section (`id="home"`, 70/30 split heading+CTA) immediately followed by a separate yellow "About Me" panel (`id="about"`, bio + photo) that scroll-expands from 80% to full bleed. Supersedes the single-photo-panel version from items 20-23.
3. Services — "Seven Services, One Point Of Contact," grouped into 3 categories: Marketing & Growth, Website & App Dev, Brand & Content. Intentional, keep the categorized structure.
4. Logo wall — grid of client company tiles, currently text placeholders (e.g. "[Client Logo — Off-Plan Developer]"), links through to `/portfolio` for the full case studies. This is the sole portfolio/trust section on the homepage (no separate industries tag row, see decision above).
5. About Me — now merged into the Hero's yellow panel (item 24), not a separate homepage section. The standalone `About.tsx` component still exists in the codebase but is no longer rendered on `/`.
6. How I Work — 4-step process: Understand the Brief, Plan & Design, Build & Launch, Optimize & Scale.
7. Growth Partnerships — segments by client type: Startups & Founders, Real Estate Developers & Agencies (UAE), In-House Teams & Growing Companies, Agencies & Consulting Partners.
8. Engagement Models — 4 packages: Project-Based, Monthly Retainer, Ongoing Partner / Dedicated Support, **Consulting & Advisory** (renamed from "Consulting & Audit" — see resolved decisions below).
9. Measurable Outcomes — 4 stat tiles plus 2 testimonial cards in one section. Testimonials are still placeholder content (see gaps).
10. Final CTA — "Book a free consultation," repeated.
11. Footer — contact details, secondary nav (Work now points to `/portfolio`).

### /portfolio (new page)

Full case-study write-ups (goal, role, execution bullets, outcome) for 5 projects: off-plan lead generation, a real estate launch campaign, continuous lead gen for agencies, a custom MERN marketing application, and a mobile app/brand/social launch. The last two still carry bracket placeholders (`[Client Industry]`, `[Add the real business goal]`) pending real client details.

## Resolved decisions (apply these in code)

1. **Package name**: rename "Consulting & Audit" to **"Consulting & Advisory"** everywhere it appears (Engagement Models section, and any pricing/portfolio references). Reason: reusing "audit" conflicts with the CTA-wording rule above, even though it's a package name and not a call-to-action.
2. **Logo wall vs. industries tags**: keep the logo wall only. Do not add a separate "industries worked in" tag row. If a future need arises to show sector coverage, fold it into the About Me or Services copy instead of adding a new homepage section.

## Forward roadmap (audited 2026-08-18, supersedes the old scattered gap lists)

Full-app audit run on 2026-08-18: build, lint, types, rendered HTML per route, schema payloads, contrast maths, bundle/asset weights, client/server split, and a live request against the lead API. Phases are ordered by what blocks what, not by effort. **Phase 0 items are live defects, not improvements.**

### Phase 0 — live defects (fix before anything else)

1. **The lead form fails for every visitor.** ~~Part (a) done 2026-08-26, see item 50 — a failed submission now offers a prefilled WhatsApp/email fallback instead of a dead error, so no lead is silently lost. Part (b), the real endpoint, still needs Bilal's URL.~~ Verified with a live `POST /api/lead`: it returns **503** because `CRM_LEAD_API_URL` is unset (there is no `.env.local`, only the example). `LeadFormPopup.tsx` then renders "Something went wrong." to the visitor. On a lead-generation site this is the single worst possible bug, and it has been live since the API route was built. Two fixes, and the first is not optional: **(a)** make the route fall back to emailing/queueing the lead instead of erroring when no CRM is configured, so a lead is never lost, and **(b)** wire the real CRM once Bilal supplies credentials. Do (a) regardless, because it is the behaviour that should exist even after the CRM is connected and the CRM is unreachable.
2. **`/portfolio` renders zero `<h1>`.** Confirmed live (`h1=0`). Its top heading comes from `SectionHeading`, which hardcodes `<h2>`. This was fixed once already via an `as` prop, but that fix lived in the branch undone by the 2026-08-17 revert, so the bug is back. Every other route correctly renders exactly 1 `<h1>`.
3. **`npm run lint` does not pass: 7 errors.** Five are `@next/next/no-html-link-for-pages` — raw `<a href="/...">` where `next/link` is required, in `Nav.tsx` (2), `services/[slug]/page.tsx` (2), and `Services.tsx` (1). This is not cosmetic: because two of them are in `Nav`, **every internal navigation on the site triggers a full page reload** instead of a client-side transition, throwing away the prefetch and SPA behaviour Next provides by default. Also one unescaped apostrophe in `services/page.tsx` and one stale eslint-disable in `WhatsAppButton.tsx`. Note `next build` does **not** run eslint, which is why these accumulated invisibly.

### Phase 1 — measurement (nothing after this is provable without it)

4. **There is no analytics or conversion tracking anywhere on the site.** Grepped the whole repo: no GA4, no GTM, no `dataLayer`, no Meta pixel, no `<script>` in `layout.tsx` at all. This is the most awkward gap in the project, because Bilal *sells* conversion tracking and server-side Conversions API setup as a service (a named section on the CRM pillar) while running none of it on his own site. Practically, it also means every SEO and copy decision in this file is unmeasurable, and item 40's keyword plan has no way to report which terms actually convert. Install GTM + GA4, define lead events for all three capture paths (`LeadFormPopup`, the WhatsApp chat flow, and direct email/phone clicks), and treat this as a prerequisite for judging Phase 3.
5. **Verify the domain in Google Search Console and Bing Webmaster Tools** and submit `sitemap.xml`. Already listed as an SEO target in this file but never confirmed done. Without it there is no keyword-position or impression data at all, so the tier list in item 40 stays guesswork rather than something that can be checked against reality.

### Phase 2 — accessibility and reachability

6. **The mega menu is keyboard-inaccessible.** *(Also a hard prerequisite for the WCAG service in Phase 3 item 13 — this is the exact criterion that service would be sold on.)* `Nav.tsx` opens it purely on `onMouseEnter`/`onMouseLeave`, with no `onFocus`, `onBlur`, `onKeyDown`, `aria-expanded`, `aria-haspopup`, or `role`. All **28 service links are unreachable without a mouse**, which fails WCAG 2.1.1 (Keyboard) against this file's own stated AA target. Add focus-driven open/close and Escape-to-close alongside the existing hover behaviour, plus the ARIA attributes.
7. **Mobile users cannot reach the 28 service links either.** The `lg:hidden` menu contains **0** of them; it only lists the 6 top-level links. Mobile visitors can only get to a specific service by landing on `/services` and drilling in. Add an expandable Services group to the mobile menu.
8. Good news from the audit, recorded so it is not re-litigated: contrast passes AA comfortably (muted `#9a9aa6` **7.19:1**, ink **18.37:1**, gold **12.61:1** on `#08080b`), all 6 `next/image` instances have real alt text, and the homepage heading hierarchy is clean (1 h1, 8 h2, 13 h3).

### Phase 3 — the keyword page build (executes item 40)

9. **8 tier-1 service pages**: `google-ads`, `meta-ads`, `website-development`, `mobile-app-development`, `ui-ux-design`, `graphic-design`, `crm-setup`, `server-side-tracking`. Content for all 8 already exists as sections inside `pillars.ts`, so this is a restructure, not new writing. This also completes the "round 2" granular sub-pages deferred in item 34 — they are the same job, not two.
10. **`/real-estate-marketing`** — the vertical page for item 40's tier 3. Highest-value unplayed card, given real developer-side experience and that property is Dubai's largest ad-spend category.
11. **`/pricing`** — "how much does X cost in Dubai" is a large query family that almost no Dubai freelancer answers publicly. Pair with suggestion-queue item 4 (AED with a USD equivalent) so it lands once, not twice.
12. **`/faq`** plus a 4-5 question homepage FAQ section. Write it around item 40's tier 4 question phrasings and the cross-border logistics in suggestion-queue item 1, since AI search matches questions rather than keywords.
13. **New service offering: "Make your website WCAG 2.2 compliant."** Bilal's own idea, added 2026-08-26. Strong fit: accessibility work sits exactly where his development and design skills overlap, it bills as both a one-off audit and a recurring retest, and it is far less contested than any marketing keyword in item 40. WCAG 2.2 has been the current W3C Recommendation since October 2023, so 2.2 is the right version to name rather than 2.1.
    - **Where it belongs**: a section on the **Website & App Development** pillar, plus its own page in the item-9 batch, which makes that batch 9 pages rather than 8. **Not** a fifth pillar — it is a deliverable within development, and promoting it would repeat the overlap mistake corrected across items 36-39. It does touch Design (contrast, focus states, semantic structure), so cross-link there rather than duplicate the content.
    - **What the service actually is**, so the page can be specific instead of vague: an automated pass (axe, Lighthouse), then the manual testing automation cannot do — real keyboard-only traversal, screen reader checks with VoiceOver and NVDA, focus order, and reflow at 320px — then a prioritised remediation list, the fixes themselves, an accessibility statement, and a retest. The manual half is what most competitors skip and the part genuinely worth charging for, since automated tooling catches only a minority of WCAG failures.
    - **Keyword value**: low competition, high intent, and nobody local is targeting it. `WCAG 2.2 compliance Dubai`, `website accessibility audit UAE`, `accessibility remediation Dubai`, `WCAG audit freelancer`, `make my website accessible Dubai`, `screen reader testing UAE`. Belongs in item 40's **tier 2** (defensible-technical) next to server-side tracking, for the same reason: most marketers cannot do it and most developers do not know why it matters.
    - **Hard prerequisite — Phase 2 must ship first.** Bilal's own mega menu currently fails **WCAG 2.2 success criterion 2.1.1 (Keyboard)**: it opens on hover with no focus handling, leaving 28 links unreachable without a mouse (Phase 2, items 6-7). Selling WCAG compliance from a site that fails a Level A criterion is an obvious credibility risk, and a prospect running axe on `bilalshafqat.com` would find it in seconds. Phase 2 stops being optional the moment this service goes live. The encouraging half of the same audit: contrast already passes AA comfortably and every image carries real alt text, so the site is close.
    - **Do not make legal claims on this page.** Sell *conformance testing against published WCAG 2.2 success criteria*, which is verifiable and demonstrable, not "legal compliance", which is not something a freelancer can warrant. Any copy implying a specific law mandates a specific conformance level should be checked with a qualified professional first, and the regulatory position for UAE private-sector sites confirmed rather than assumed. Stating the conformance level actually reached (A / AA / AAA) with the evidence behind it is honest and sufficient.
    - **Price it as a retainer too**: conformance decays with every content update, so a quarterly retest is a natural recurring engagement rather than a one-off audit.
14. **Restructure `/portfolio` into unified brand case studies (replacing the 5 task-level write-ups).** Bilal's own strategic call, 2026-08-26, and it is the right one: the current page lists 5 deliverable-shaped entries ("Lead Generation for Off-Plan Properties", "Mobile App, Brand & Social Launch"), which reads as a task list. Grouping by *brand* instead of by *deliverable* is what demonstrates full-funnel ownership. One page per brand, never one page per asset — a separate page for "Mövenpick Social", "Mövenpick Landing Page" and "Mövenpick Email" would dilute topical authority three ways and position him as a task executor rather than a growth partner.
    - **Three brands confirmed, all with the same three-channel shape** (social/paid + landing page + email/CRM), which is exactly what makes the grouped format work:
      | Brand | Sector | Angle |
      |---|---|---|
      | Mövenpick | Hospitality | Integrated digital marketing & direct booking funnel |
      | Oceara / Oceara Park View Residences (Refine / PPS Properties) | Off-plan real estate, Dubai Islands | End-to-end off-plan lead generation engine, HNW investor targeting |
      | Hadley Heights (Leos Development) | Real estate | Landing page, social campaign, email marketing |
    - **The 4-part template each case study follows**: (1) Challenge & goal, (2) the full-stack solution split into the three pillars he personally executed — ad strategy & social content, landing page build (UI/UX + Next.js/MERN + speed), email & CRM integration, (3) a visual showcase pairing full-width landing page screenshots with social creatives and email templates, (4) 2-3 stat badges. Deliverables render as tabs or a unified gallery **on one page**, not as separate routes.
    - **Logo wall becomes the entry point**: monochrome vector logos under one headline ("Trusted by leading hospitality, real estate and enterprise brands across the UAE"), each logo clicking through to that brand's case study. This finally gives the logo wall from the project's original hard rules a real destination instead of 5 text placeholders.
    - **Stat badges must stay honest.** Where real numbers are not shareable, use verifiable capability claims (fast landing page load, automated CRM sync, multi-channel lead pipeline) rather than invented percentages. No fabricated metrics, ever — this is the same constraint that keeps `Review` schema unbuilt.
    - **Permission check before publishing.** Oceara is a Refine project and Refine is Bilal's employer; Hadley Heights belongs to Leos Development. Named client work, logos, and screenshots need explicit sign-off before going live, and anything commercially sensitive (spend, CPL, contract terms) should be left out or aggregated. Confirm this per brand rather than assuming.
    - Asset collection is tracked in Phase 4 — Bilal is gathering screenshots, creatives and email templates now. The page structure and template can be built ahead of the assets landing.
15. **Productised entry-offer pages (small, fixed-scope services).** Extends the WCAG page in item 13 into a proper set. These convert far better than a generic "hire me" page because the buyer knows exactly what they get, and each one is a separate low-competition keyword target. Recommended set, ordered by fit:
    | Page | Why it works | Best market |
    |---|---|---|
    | WCAG 2.2 accessibility audit (item 13) | Legal-risk driven demand, few competitors | **UK / US / Canada** |
    | Tracking & analytics audit (GA4 + pixel + Conversions API health check) | His sharpest differentiator; most agencies cannot do it | **UK / US / Canada** |
    | Core Web Vitals / site speed audit | Objective, tool-verifiable, easy to price | **UK / US / Canada** |
    | GA4 + GTM + server-side tracking setup | Fixed-scope build, fully remote | **UK / US / Canada** |
    | Website SEO audit | High search volume, buyer-recognised | UAE |
    | Paid campaign audit (Google / Meta) | Direct route into retainer work | UAE |
    | Landing page in 7 days (Next.js) | Concrete deliverable, concrete price | Either |
    | Conversion / landing page audit | Natural upsell after any traffic work | UAE |
    - **Resolves a conflict with this file's own hard rules, deliberately and on Bilal's instruction.** The CTA rule bans the word "audit" because he did not want to be boxed in as an SEO specialist. He explicitly asked for audit pages on 2026-08-26. Resolution: the ban stays for the **site-wide CTA**, which remains "Book a free consultation" everywhere; "audit" is allowed as a **productised service name** on its own page. The original concern was about positioning, and a page selling a defined deliverable does not box him in the way a single global CTA would. See the amended hard rule above.
    - Each page needs a real scope, a real turnaround, and a price or price range. An entry offer without a price is just another contact form.
16. **Hosting, sold as a managed care plan rather than as hosting.** Bilal's request, 2026-08-26. The recurring-revenue case is strong and it attaches naturally to every site he builds, but this is the first service on the list that carries **ongoing operational liability**, so it needs framing and boundaries the audits do not.
    - **Do not sell "hosting" as a line item.** Reselling Vercel, DigitalOcean or AWS means marking up someone else's infrastructure at thin margin, while inheriting all of the responsibility. Sell a **managed care plan** instead, where hosting is one component: hosting + SSL + automated backups + uptime monitoring + dependency and security updates + a small monthly change allowance + a monthly performance report. Same underlying cost, several times the perceived value, and it sets support expectations explicitly.
    - **This is the recurring wrapper the rest of Phase 3 is missing.** Every one-off in item 15 decays: WCAG conformance breaks with the next content edit, tracking pixels break when platforms change, Core Web Vitals regress as pages grow. A care plan is the honest answer to all of them and converts project work into retainer revenue. Pair it with the "Landing page in 7 days" offer as the default next step after launch.
    - **Risks to accept deliberately before offering it**, because they are real and none of them apply to an audit:
      - **Uptime becomes his problem, permanently.** A solo freelancer has no on-call rotation. A client's site going down mid-campaign at 3am is his to answer. Audits and builds end; hosting never does.
      - **Scope creep is near-guaranteed.** Hosting clients raise unrelated issues ("my email is broken"). The care plan must state in writing what is *not* included.
      - **Stack choice changes the risk profile enormously.** Next.js and static sites on a managed platform are low-maintenance. Hosting WordPress means inheriting every plugin vulnerability on every client site. Recommend restricting the offer to sites he built himself, at least initially, rather than taking over arbitrary legacy installs.
      - **An SLA is required, even a modest one.** Without a stated response window and maintenance window, expectations default to 24/7. State them plainly, and price accordingly.
    - **Data residency and UAE PDPL need checking, not assuming.** Some UAE clients — regulated sectors and anything government-adjacent especially — may require data to stay in-region, which constrains hosting location. This interacts with UAE PDPL obligations around personal data in forms and CRMs. Confirm the position with a qualified professional before making any commitment in writing; do not state compliance in page copy.
    - **International note** (see the cross-cutting section below): hosting itself travels fine, since the platforms are global. Support **hours** do not. A UK client overlaps most of Bilal's working day; a US Pacific client does not. Any SLA offered to North American clients should be async-friendly and say so, rather than implying live coverage.
    - **Where it lives on the site**: a page in the item-15 set, plus a real presence in the existing **Engagement Models** section, where it fits under Monthly Retainer. Price it off actual measured infra cost plus margin — do not publish a figure until the underlying platform cost per site is known, and quote in AED with a USD equivalent per the dual-currency prerequisite.
    - **Sequencing**: deliberately after the audits, not before. Audits are pure upside with a defined end; this one adds permanent obligation, so it should launch once there is a steady client base worth retaining.
17. **Blog** — the last unbuilt route from this file's own sitemap. Deliberately last: it is the highest-effort, slowest-returning item, and the 2026-08-17 session already established that thin migrated posts were worse than none.

### Phase 4 — trust content (blocked on Bilal, cannot be built without him)

18. **Real testimonials** — 2 placeholders in `Results.tsx`. Also the gate on `Review` JSON-LD, which stays deliberately unbuilt because fabricated review markup is actively penalised.
19. **Real client logos** — 5 placeholders in `LogoWall.tsx`. Now has named targets: Mövenpick, Oceara / Refine, and Leos Development (Hadley Heights), as monochrome vectors. Needed for the logo-wall entry point in Phase 3 item 14.
20. **Case study assets** — Bilal is actively collecting these as of 2026-08-26. Per brand (Mövenpick, Oceara, Hadley Heights): landing page screenshots at full width, social/paid ad creatives, email template screenshots, and any shareable result figures. Supersedes the older "2 unfinished case studies" note, since Phase 3 item 14 replaces all 5 existing write-ups rather than finishing 2 of them. Also needs written permission per brand before publishing (Oceara is a Refine project, Hadley Heights is Leos Development).
21. **CRM credentials** — endpoint URL, auth token, and expected field names. Unblocks Phase 0 item 1(b) and an inline form on `/contact`.
22. **CallMeBot API key** — the last open piece of item 12, for a silent WhatsApp push to Bilal on each new lead.
23. **Confirm two things written on his behalf**: the "reply within one business day, same day on WhatsApp" commitment on `/contact`, and which languages to list in `availableLanguage` (currently English only, because inferring more would have been a guess).

### Phase 5 — local and Maps visibility

24. **Google Business Profile**, set up as a service-area business. Maps ranking comes almost entirely from a verified listing, not from the site, so no amount of on-page work substitutes for this. `/contact` now carries `ContactPage` + `Person` schema with both phone numbers and a `PostalAddress` for the listing to reconcile against.
25. Add `telephone` to the global `Person` schema in `StructuredData.tsx`, and evaluate whether a `ProfessionalService` type alongside `Person` is warranted once the listing exists.

### Phase 6 — performance and hygiene

26. **Two oversized hero images**: `bilal-shafqat-coat.avif` (666KB) and `bilal-shirt.avif` (568KB), both loaded as CSS `background-image` and therefore bypassing `next/image` entirely — no responsive sizing, no lazy loading, full weight on every visit. This was a deliberate trade in item 30 to get literal `background-size` control, and it is now the main risk to this file's own LCP-under-2.0s target. Generate properly sized variants rather than reverting the crop control.
27. **`llms.txt` is stale** — zero mentions of `/about` or `/contact`, and it carries no URL index at all. It also drifts from `pillars.ts` by hand, the same failure mode as item 33. Refresh it and add a page list.
28. **No custom 404** — using the Next default. The 2026-08-17 session built one and the revert removed it.
29. **Harden `/api/lead`** once it is actually live: it has a honeypot and env-based auth with no hardcoded secrets (good), but no rate limiting and no email-format validation.
30. **Delete `About.tsx`** — the only genuinely unreferenced component (the three `Hero*` files are still used by the preview routes).

### Phase 7 — deferred, revisit when Phases 0-4 are clear

31. Remaining suggestions-queue items not folded in above: plain-language subtitles on technical services (5), privacy policy page (6), regions-served line (7), trust/partner badges (8), Arabic version with RTL (10). Item 3 (time zone) closed on 2026-08-18 via `/contact`; item 2 (WhatsApp) closed in item 13.
32. Retire or repurpose the 3 `preview-hero-*` routes. They are `noindex` as of item 41 so they are no longer an SEO risk, only clutter.

### Cross-cutting: targeting UK, US and Canada (added 2026-08-26)

Bilal stated on 2026-08-26 that he wants clients in the UK, US and Canada. This is a real strategic shift, not an add-on, and it partly invalidates work already in this file. Recording it honestly rather than layering it on top.

- **The entire item-40 keyword plan is Dubai-scoped and does not reach these markets.** Every tier 1 term ends in "Dubai" or "UAE". `freelance web developer Dubai` will never surface for a buyer in London or Toronto — Google serves local intent locally. An international push needs its own keyword set, not a translation of the existing one.
- **Location keywords stop working, so specialism has to do the work instead.** Competing on generic global terms (`freelance web developer`, `digital marketing consultant`) against every agency and freelancer on earth is unwinnable. The winnable route is narrow technical specialism where the buyer is searching for a capability, not a city: WCAG/accessibility remediation, Meta Conversions API and server-side tracking, GA4 migration, Core Web Vitals. That is exactly why item 15's table marks those four as the UK/US/Canada plays.
- **Accessibility is the strongest of these by a wide margin in Western markets.** Demand there is driven by legal exposure that simply does not exist in the same form locally — the ADA in the US, AODA in Ontario, the Equality Act in the UK, and the European Accessibility Act. That makes item 13 arguably a *Western-market* service that happens to also be sellable in the UAE, rather than the reverse. **Do not state legal requirements as fact in page copy**; name the frameworks as context, sell conformance testing, and have the specifics checked by a qualified professional (same constraint as item 13).
- **Sell what travels; keep local what does not.** Remote-deliverable specialist work (audits, tracking builds, accessibility remediation, landing pages) crosses borders fine. Relationship-and-timezone work (social media management, ongoing campaign management, anything needing daily contact) is much harder to win from Dubai and is better kept aimed at the UAE and GCC.
- **Timezone is an asset for the UK and a liability for North America.** Dubai is UTC+4, so a UK client overlaps for most of their working day. US Pacific barely overlaps at all. Say this plainly rather than hide it: lead with UK/Europe, treat US and Canada as async-friendly project work rather than retainers needing live calls.
- **Three queued suggestions stop being optional and become prerequisites**: dual-currency pricing (suggestion 4, AED with USD/GBP), the regions-served line (suggestion 7), and the cross-border FAQ covering payment method, contracts and NDAs for fully remote clients (suggestion 1). An overseas buyer will not enquire without these answered.
- **`/contact` needs revisiting for this audience.** It currently states GST (UTC+4) and Monday to Friday, which is correct for the UAE but reads as a limitation to a Western buyer. Add explicit overlap hours rather than only a timezone label.
- **Off-site presence matters more here than the site does.** For UK/US/Canada buyers, Upwork, Toptal-style platforms and LinkedIn will produce enquiries far sooner than organic ranking on a new domain. Align those profile headlines to the specialist terms above, not to the Dubai-scoped ones.

## Suggestions queue — professionalism & clarity for UAE + global clients (not yet actioned, review on request)

Bilal asked for a running list of ideas to make the site read more professional and easier to understand for clients both inside the UAE and abroad. None of these are built yet — surface this list when he asks what's queued up.

1. **FAQ section, written for cross-border logistics** (extends the existing FAQ gap above): cover payment/currency (AED for UAE clients, USD wire/Stripe for international), time zone expectations, response times, and how contracts/NDAs work for fully remote clients. This is the single highest-leverage item for reducing "will this actually work for me" hesitation from overseas clients.
2. **WhatsApp click-to-chat button**: WhatsApp is the default business channel in the UAE. A lightweight WhatsApp icon alongside (not replacing) the existing lead-form popup would meet local clients on their preferred channel without adding a competing CTA style.
3. **Time zone transparency line** — **done 2026-08-18.** Live on `/contact` as "Gulf Standard Time (GST, UTC+4), Monday to Friday" with an expected reply window. See item 42, and note the reply-time wording still needs Bilal's confirmation.
4. **Dual-currency pricing signals**: once real package pricing exists, show an indicative starting price in AED with a USD equivalent alongside it in Engagement Models, so visitors from either region immediately gauge budget fit without emailing first.
5. **Plain-language subtitles on technical services**: a one-line, jargon-free translation under technical service names (e.g. "MERN Stack Development" → "custom web apps built around your exact workflow") so non-technical decision-makers, common among international SMB clients, understand the value instantly.
6. **A short Privacy Policy page**: now that the site actively collects lead data into a CRM, a plain-language note on what's collected and why is a baseline professionalism signal for both UAE (PDPL-aware) and international (GDPR-aware) visitors.
7. **Regions-served line**: a simple statement like "Working with clients across the UAE, GCC, Europe, and North America" (only if true) — reassures international visitors he isn't purely local, and reassures UAE clients of his local grounding.
8. **Trust/partner badges**, if Bilal holds any (e.g. Meta Business Partner, Google Partner) — a fast credibility signal for clients comparing a solo freelancer against an agency.
9. **Real testimonials** (already tracked above as a gap) — flagged again here specifically as the top trust signal for international clients who can't easily verify a solo operator's credibility otherwise.
10. **Arabic-language version, longer-term**: worth considering given the GCC client base, but this is a bigger scope item (proper professional translation, RTL layout support) — not a quick add, only pursue once the English site and core content gaps are settled.

### New items added 2026-08-01

11. **Footer social icons** — **done.** Trimmed from 9 placeholder platforms down to the 4 Bilal actually confirmed: LinkedIn, Instagram, Facebook, Behance. Replaced the inconsistent text abbreviations with real icon glyphs (inline SVG for LinkedIn/Instagram/Facebook, a clean "Be" monogram for Behance — no new dependency). All 4 still link to `#` placeholders; still need Bilal's real profile URLs to swap in.
12. **AI assistant chat widget** — queued, not yet built. Auto-opens once per session after the visitor scrolls past the end of the 2nd homepage section (Services). Collects name, email, phone, service of interest, and a meeting time, then sends the data to the CRM (same pipeline as the lead popup, `/api/lead`) and pushes an immediate WhatsApp notification to Bilal at +971 52 976 6006 so he can follow up personally. Decisions made so far:
    - **Meeting time field: confirmed — simple datetime-local input** (not a Cal.com/Calendly embed). Captures the visitor's preferred slot in their own browser timezone; Bilal reconciles manually. Faster to ship, timezone conversion is on Bilal to handle for now.
    - **WhatsApp delivery to Bilal: confirmed — CallMeBot fast path.** Bilal needs to message CallMeBot's activation number once to get a personal API key (free, no business verification) before this can be wired up.
    - **Still open**: the widget's opening framing/copy — recommended line is "Hi, I'm here on Bilal's behalf — want me to grab a few details so he can reach out personally?" so it's honest that a real person follows up rather than the bot pretending to be one. Confirm or adjust this copy before build.
    - Needs from Bilal before build: the CallMeBot API key (once generated), and a yes/adjust on the opening copy above.
13. **WhatsApp click-to-chat button** — **done, then upgraded to a lead form (2026-08-02).** Originally a direct `wa.me` link; Bilal asked to collect fields first and send the data two ways. Now: clicking the floating green button opens a form (name, email, phone, service dropdown, preferred meeting time — a plain datetime-local input), and on submit it does two things at once — opens WhatsApp (`wa.me`) with all the details pre-filled as a message to +971 52 976 6006 ready for the visitor to hit send, and separately posts the same data to `/api/lead` (source: `whatsapp-popup`) as a best-effort CRM copy that doesn't block or affect the WhatsApp flow if the CRM isn't configured yet. No CallMeBot or WhatsApp Business API needed for this — delivery is via the visitor's own WhatsApp app/web, same mechanism as the original button, just with real data instead of a canned line. Tested end to end (headless browser): form opens, WhatsApp deep link carries all 5 fields correctly formatted, CRM POST fires in parallel without blocking the WhatsApp tab. This substantially overlaps with item 12's field list (name/email/phone/service/meeting time) — item 12's distinguishing features that remain open are the **auto-open-on-scroll trigger** and a **silent CallMeBot-based notification to Bilal** (vs. this flow, which is visitor-initiated and requires them to hit send in WhatsApp themselves).

14. **WhatsApp popup redesigned as a step-by-step chat (2026-08-03)** — **done.** Bilal asked for the static form to feel like chatting with a real person instead. `WhatsAppButton.tsx` now runs a proper conversational flow: one question at a time in a chat transcript ("Bilal's Assistant" header, bot bubbles left/muted, user answers right/green), a short typing-indicator pause between questions, and the service step renders as tap-to-answer chips instead of a dropdown. The opening line is deliberately honest — "I'm here on Bilal's behalf so he can personally follow up with you" — rather than having the bot imply it's Bilal himself typing live. On the final answer it does the same dual-send as before (opens the WhatsApp deep link with all 5 answers formatted, and posts to `/api/lead` with source `whatsapp-chat`), then ends with a friendly closing bot message and a Close button.
15. **Widget redocked + proactive auto-open (2026-08-03)** — **done.** Bilal flagged that the centered modal felt like an interrupting popup rather than a real chat widget, and asked for it to auto-open like a sales rep greeting a visitor. Changes: the panel is now docked bottom-right (`bottom-40`, above the launcher and the "Quick Enquiry" button, no overlap), no dark backdrop, and the page stays fully visible and scrollable behind it — confirmed the visitor can still scroll the site while the chat stays open. The launcher button doubles as open/close (icon swaps to an X, plus a small unread dot before first interaction). It also now opens itself once per browser session after the visitor scrolls ~700px down, leading with a warm two-part greeting ("Hi there! Thanks for stopping by — how can I help you today?" → then the name question) rather than jumping straight to data collection — mimicking a rep stepping in rather than a static widget. Manual clicks on the launcher skip the extra greeting line and go straight to the name question, since clicking already signals intent. Tested end to end: confirmed closed on load, confirmed it opens itself with both greeting messages after scrolling past the threshold, confirmed the page remains scrollable/interactive while the panel is open. This closes out the auto-open-on-scroll piece of the old "AI assistant chat widget" idea (item 12) — the only remaining distinct piece of that idea is a silent CallMeBot push straight to Bilal's phone (this flow still requires the visitor to hit send in WhatsApp themselves).
16. **Meeting-time step removed + trigger fixed + no-gatekeeping escape hatch (2026-08-03)** — **done.**
    - Dropped the "preferred meeting time" question entirely. The flow is now 4 steps: name, email, phone, service.
    - Auto-open trigger fixed to match what Bilal asked for: whichever happens first — the visitor scrolling past the Services section (checked via `#services`' position, falls back to a 700px scroll distance on pages without that section, e.g. `/portfolio`) — or 30 seconds elapsed on the page.
    - Bilal flagged that requiring the full Q&A before reaching WhatsApp could feel like a gate blocking access to "a real person." Added a small "Prefer to skip ahead and message Bilal directly?" link, visible under the input at every step from the very first question onward. Clicking it immediately opens WhatsApp with whatever partial details were given so far (or just the generic opener if none), so no one is forced through the full flow to reach a human.
    - Tested end to end: confirmed the flow ends after the service step with no meeting-time question, confirmed the skip-ahead link opens WhatsApp correctly even with zero fields answered.
17. **Services section rebuilt as a numbered hover-list with crossfade preview (2026-08-03)** — **done.** Reference: tentwenty.com's services section (verified live — a hover-driven numbered list with a single preview image that crossfades per row). Also checked landonorris.com per Bilal's request: it runs bespoke WebGL/canvas background art and a scrapbook photo masonry, which doesn't fit this site's B2B tone or scope, so nothing from it was adopted beyond confirming the idea of a section-to-section background color shift (not yet built, low priority). `Services.tsx` now shows a numbered list (01–07, grouped under the existing Marketing & Growth / Product & Engineering / Brand & Content headers) on the right, and a sticky preview panel on the left that crossfades (Framer Motion `AnimatePresence`) between each service's icon, description, and bullet chips on hover or tap — tap works for mobile since there's no hover there. No content was dropped; the same descriptions/bullets from the old card grid now live in the preview panel. Verified live: hovering different rows correctly swaps the panel's accent color, icon, and copy, confirmed on both a Marketing & Growth item and a Brand & Content item.
18. **Animated glow blobs + shrinking nav on scroll (2026-08-03)** — **done.** Reference: a SocialLift template screenshot Bilal shared showing a large glowing purple radial blob behind the CTA button, plus a request for a full-width header that narrows once scrolled.
    - The shared `.blob` class (used in Hero and Contact) now drifts and pulses via a `blob-float` CSS keyframe (14s loop, respects `prefers-reduced-motion`) instead of sitting static. Existing blobs got staggered `animationDelay` values so they don't pulse in sync. Added one larger, more saturated violet blob centered behind Contact's CTA card specifically, to more directly echo the reference image.
    - `Nav.tsx`: at the top of the page the bar is now full-width and flat (no pill background); past the existing 12px scroll threshold it animates down to the previous centered `max-w-7xl` glass pill. Verified via bounding box measurement: 1376px wide at top vs 1232px wide scrolled, on a 1440px viewport — confirmed the shrink is real, not just visual.
    - Honest caveat: the glow reads more subdued than the reference because it sits behind Contact's dark glass card (`glass-strong`, translucent), whereas the reference has no card in front of it. If Bilal wants it more vivid/dominant, it needs either a higher blob opacity or a less opaque card background — flagged, not changed without asking.
19. **Readability pass on Engagement + WhoIWorkWith (2026-08-04)** — **done.** Bilal shared a minimalist reference site ("Viral") and initially asked for a full rebrand to its light/cream color scheme; after a comparison table walked through what that would actually mean (every component, brand identity, the glow/glass effects all rebuilt), he clarified he wants to **keep the current dark/gold colors** — the real complaint was that the reference's cards read more clearly than his own, purely from spacing and content density, not color.
    - Root cause identified concretely (screenshotted both before making any change): the Engagement and WhoIWorkWith cards stacked icon → title → description → a labelled bullet list (or two, in Engagement's case) → an italic summary line — 4 layers of content per card, 4 cards wide. The reference's cards show icon → title → one line, nothing else.
    - Fix: kept every word of existing content (none of it was cut), but moved the detailed bullet lists behind a "See details" toggle per card (Framer Motion height animation) so the default view only shows icon, title, description, and the italic "best for" line. Confirmed live: both sections now fit far more comfortably in one view, and expand/collapse animates smoothly with no console errors.
    - Scope stayed limited to these two sections since they were the concretely identified offenders — Hero, Services, and other sections were checked and are not similarly dense.
20. **Hero replaced with an Anthropic-inspired layout, now live on the homepage (2026-08-06)** — **done.** Bilal referenced anthropic.com's homepage specifically; confirmed live (GSAP + ScrollTrigger genuinely active, 3 instances) before building anything, rather than guessing from memory. Adopted the general technique and layout pattern, not their copy or colors — this is `HeroAlt.tsx` (previously piloted at `/preview-hero-alt`, now swapped in as the live Hero on `page.tsx`, replacing the old `Hero.tsx`).
    - Top area: heading on the left (Bilal's existing headline, restyled — large bold 64px-equivalent type, underline emphasis on key phrases instead of the gold text-gradient) with supporting paragraph + both CTAs ("Book a free consultation" primary, "View my work" secondary) on the right, matching Anthropic's split-heading-and-content structure and type scale.
    - Below: the photo panel starts at 90% width with rounded corners and genuinely animates to a full-bleed 100% square-edged panel as the visitor scrolls, driven by real GSAP `ScrollTrigger` (`gsap` added as a new dependency) — verified by measuring the panel's actual pixel width at multiple scroll positions (not just visually), confirmed it animates smoothly from ~90% to exactly 100% and holds once past the trigger range.
    - Fixed two real bugs surfaced while making this live rather than just a preview: the section was missing `id="home"` (nav's Home link and logo click depend on it) and was missing the secondary "View my work" CTA entirely (only had one button) — both restored before going live. Also fixed the heading initially rendering underneath the fixed nav bar (insufficient top padding).
    - Verified end to end on the actual homepage (not just the preview route): both CTAs present with correct hrefs (`#contact`, `/portfolio`), nav's Home link scrolls correctly, scroll animation reaches a clean full 1440px on a 1440px viewport, mobile layout stacks cleanly, zero console errors.
    - **Content dropped in this version, flagged not silently lost**: the scrolling skills ticker and the inline "15+ Years / 6 Services / Dubai, UAE" stat row that the old Hero had are not present here — the stats now only appear as overlay tags on the photo panel. Add back if Bilal wants that content restored.
    - `/preview-hero-alt` route still exists (compares old `Hero.tsx` against this design) but is now comparing against what's no longer live — low priority to clean up or repurpose.
21. **Hero copy rewrite, logo/favicon wired in, node-network hero visual, nav border bug, and site-wide SEO/GEO build-out (2026-08-07)** — **done.**
    - Headline changed from "One partner for everything from paid ads to shipped product" to "15 years of marketing, design, and development experience — in one senior partner" — Bilal felt the old phrasing read like a dropshipping/ecommerce-hustle pitch rather than a senior consultant. New supporting paragraph emphasizes working directly with clients, no account managers or junior staff.
    - Real logo wired in (`public/logo/bs-logo.svg` wordmark in Nav and Footer, `public/logo/bs.svg` icon mark as the actual favicon via Next's `icon.svg` file convention — had to delete the old default `favicon.ico`, since Next prioritized that file-based convention over the `metadata.icons` setting alone).
    - Nav border bug fully root-caused, not just patched: it wasn't a static leftover border, it was `.glass-strong`'s own 1px border briefly reasserting itself mid-transition while scrolling back up, since the first fix only zeroed the border in the settled "scrolled" state. Fixed by pinning `border-width: 0` unconditionally regardless of scroll state; verified by sampling computed border every 50ms through the full reverse-scroll transition, confirmed 0px at every sample.
    - Hero's photo replaced with an animated "connected service nodes" visual per Bilal's request (inspired by a heynesh.com screenshot he shared, rebuilt from scratch with Bilal's own service names/icons, not their actual code, logo, or testimonials) — 6 floating cards (Paid Social Media Marketing, Performance Marketing, App Development, App Design, Web Development, Social Media Management), each bobbing independently via a new `.node-float` CSS keyframe, joined by thin SVG lines into a loose network shape over the existing grid/glow background.
    - Site-wide SEO/GEO audit done by actually searching the codebase, not assuming: confirmed home + `/portfolio` had metadata, photos had real alt text, but there was **no robots.txt, no sitemap, no llms.txt, and zero structured data anywhere**. Built all four: `public/robots.txt` (explicitly allows GPTBot, ClaudeBot, Google-Extended, PerplexityBot, CCBot), `src/app/sitemap.ts` (auto-generates `/sitemap.xml`), `public/llms.txt` (plain-text site summary for AI crawlers), and `StructuredData.tsx` (Schema.org `Person` + `Service` JSON-LD, using `Person` rather than `Organization` since Bilal operates under his own name, not a separate company brand — includes his real Behance URL as `sameAs`, no fake reviews/testimonials since none exist yet). Added `metadataBase` and per-page canonical URLs. Verified all of it actually renders (curled robots.txt/llms.txt/sitemap.xml, grepped the live HTML for the canonical tag and full JSON-LD payload).
    - **Needs confirmation from Bilal**: all of the above assumes the production domain is `bilalshafqat.com` (from his Behance profile) — if the real domain differs, it only needs correcting in `layout.tsx` (`metadataBase`), `sitemap.ts`, `robots.txt`, `llms.txt`, and `StructuredData.tsx`.
    - **Still open, not built**: `FAQPage` and `Review` JSON-LD schema — deliberately skipped since there's no real FAQ content or real testimonials yet; adding fake review schema would be actively harmful (Google penalizes fabricated review markup), so this should only be built once item 9's placeholder testimonials are replaced with real ones.
    - Bilal also asked about a small text element appearing above/before the hero section, referencing anthropic.com — checked anthropic.com again and did not find such an element on their actual current homepage (nav flows straight into the hero). Flagged back to Bilal to clarify what he's referring to rather than guessing.
22. **GSAP load-in entrance sequence + photo brought back with a giant wordmark (2026-08-07)** — **done, hero section only, nothing else touched per Bilal's explicit instruction.** Bilal studied a reference site frame-by-frame (heynesh.com, shared earlier) and had another Claude session turn that study into a precise technical spec, which was implemented faithfully with Bilal's own name, photo, and colors, not the reference's actual code, logo, or copy.
    - Brought the photo back (it had been swapped for the floating-node visual two sessions ago) and added a giant decorative "BILAL" wordmark in gold behind it, `aria-hidden="true"` so the real `<h1>` stays the actual heading for accessibility/SEO. Sized via `clamp()` so it genuinely extends past the photo's edges on both sides — first attempt (`clamp(4rem, 18vw, 14rem)`) sized it almost exactly as wide as the photo (626px vs. the photo's ~630px), completely hiding it; caught this by measuring actual rendered bounding boxes rather than trusting the screenshot alone, then sized up to `clamp(6rem, 32vw, 30rem)` and confirmed the letters now visibly peek out on both sides.
    - Built the full sequence as a single `gsap.timeline()` via `@gsap/react`'s `useGSAP` hook (new dependency): wordmark scales/fades in first, then the photo focus-in (blur 30px → 0, scale 0.94 → 1), then headline, paragraph, stat badges, and CTAs cascade in with staggered `"<0.1"`-style position params, each blurred+offset until its turn. Gated on the photo's `onLoad` firing (not `onLoadingComplete`, which is deprecated in this Next.js version) so the timeline can't start before the asset is actually visible.
    - Respects `prefers-reduced-motion`: confirmed via Playwright with `reducedMotion: "reduce"` that the headline group's opacity is already `1` at 300ms in, far before the ~2s animation would normally finish, meaning it skips straight to final state rather than animating.
    - The pre-existing scroll-driven width expansion (90% → 100% panel) from item 20 is untouched and still works: confirmed the panel measures ~1350px settled, then genuinely reaches a clean 1440px (full width) after scrolling, same mechanism as before, now coexisting with the new load-in timeline via a second, independent `useGSAP` call.
    - Real content stays present in the DOM at first paint — only opacity/filter/transform are animated, nothing is hidden via CSS classes — so this shouldn't hurt LCP or hide content from crawlers/no-JS visitors, per Bilal's own spec requirement.
23. **Wordmark removed, reverted to the single-photo composition (2026-08-07)** — **done.** Bilal saw item 22 live and said the hero "doesn't look good." Rather than guess at a fix, gave an honest diagnosis: the giant wordmark is a bold "creative portfolio" flex that fits a flashy Webflow-developer personal brand, but fights against Bilal's actual positioning ("15 years, senior, no-nonsense partner") — the loud gold lettering competes with the headline instead of reinforcing credibility. Recommended dropping the wordmark and returning to the calmer single-photo-with-floating-stat-card composition Bilal had responded well to earlier, while keeping the new GSAP entrance choreography from item 22. Bilal confirmed. Removed the wordmark entirely (and its now-unused ref), restored the photo to full-bleed `object-cover` within the panel, restored the "15+ years" floating stat card top-right and the Dubai/6-Services tags bottom-left. Entrance timeline simplified to match: photo focus-in is now step 1 (no wordmark step before it), headline/paragraph/stats/CTA cascade in after, same stagger technique as before. Confirmed live: clean composition, no console errors, entrance animation settles correctly.
24. **Split black-headline + yellow-About-box hero, now live on the homepage (2026-08-08)** — **done.** Bilal pivoted the hero to two visually separate blocks: a plain black top section keeping the existing 70/30 headline+CTA split (`id="home"`, untouched from before), and a standalone yellow "About Me" panel below it (`id="about"`) with the bio content and photo, that starts at 80% width/height and scroll-expands to full bleed via GSAP `ScrollTrigger` — piloted first at `/preview-hero-yellow` (`HeroYellowTest.tsx`) before going live, per the established pattern of testing riskier layout changes in isolation first.
    - **Width fix**: the yellow panel's initial width now matches the black headline's container exactly, rather than an arbitrary 80% of viewport. Changed from inline `style={{ width: "80%" }}` to `className="mx-auto max-w-7xl"` (same container class the heading uses), removing the width override from the GSAP tween. Verified by measuring both containers on the live page: heading container 1280px, yellow panel 1280px, exact match.
    - **Typography upgrade sitewide**: bumped the shared `SectionHeading.tsx` component (used by Services, LogoWall, Process, WhoIWorkWith, Engagement, Results) from `text-3xl sm:text-4xl` to `text-4xl sm:text-5xl` for H2s, with the description text and max-width container sized up to match — this single change propagates larger, more confident headings across every major homepage section. The hero's own H1 was sized up to `text-6xl sm:text-7xl lg:text-[4.5rem]`, and the yellow panel's H2 to `text-3xl sm:text-4xl lg:text-5xl` — following standard type-scale conventions (roughly 1.25–1.33x steps between heading levels) rather than arbitrary sizes.
    - **Went live**: copied the finalized preview component into `HeroAlt.tsx` (the actual live hero, replacing the previous single-photo-panel version from items 20-23) and removed the now-redundant standalone `About` section from `page.tsx`, since its photo+bio content now lives inside the yellow panel. `About.tsx` itself was left in place, unused, not deleted. Preserved `id="home"` and `id="about"` on the two new sections so Nav's existing anchor links keep working without changes.
    - Verified end to end on the live homepage (not just the preview route): `npm run build` succeeded cleanly; confirmed via Playwright that both section IDs exist, the width match holds (1280px = 1280px), and there are zero console/page errors.
    - `/preview-hero-yellow` and the earlier `/preview-hero-wordmark` routes still exist but are now stale/superseded — low priority to clean up, consistent with not deleting exploratory work that might be revisited.
25. **Modern type scale (h1 56px / h2 44px) sitewide, and a real bug fix to the scroll-grow effect (2026-08-10)** — **done.** Bilal asked for exact desktop sizes matching Anthropic's own scale (h1 56px, h2 44px) rather than the larger 72px/48px sizes from item 24 — that item had overshot "bigger" into oversized territory.
    - Set H1 (hero headline, `HeroAlt.tsx` + `HeroYellowTest.tsx`) to `text-4xl sm:text-5xl lg:text-[3.5rem]` (56px desktop). Set every true H2 sitewide (`SectionHeading.tsx`, plus `Results.tsx` and `Contact.tsx`, which don't use the shared component, plus the yellow panel's own H2) to `lg:text-[2.75rem]` (44px desktop), each with a proportional mobile/tablet ramp underneath. H3s got a smaller proportional bump: card-title H3s in `Services.tsx` and `CaseStudies.tsx` moved toward a ~32px step, and the compact 4-column card titles in `Process.tsx`, `WhoIWorkWith.tsx`, `Engagement.tsx` went from a flat 18px to 18px/20px responsive, deliberately kept smaller than a full H3 jump since bumping them to 32px would have broken those tight grid layouts. Verified all sizes via computed-style measurement on the live page, not just visually.
    - **Real bug found and fixed**: the yellow panel's "grows to full-bleed on scroll" animation (originally from item 20, carried into item 24) had been silently broken by item 24's own width-match fix — adding the `max-w-7xl` class to match the heading's width also permanently capped the panel's max-width, so the GSAP tween's `width: "100%"` target could never exceed 1280px. Measured directly: panel width was 1280px before, during, and after scrolling — no growth at all. Fixed by dropping the `max-w-7xl` class in favor of an inline `width: "min(80rem, 100%)"` (mathematically identical at load, but not a hard cap), so the same tween can now genuinely grow past it.
    - **Second bug found while fixing the first**: once width could move again, measurement showed the panel wasn't starting at a clean 0%, either. It sits close enough to the top of the page that the element-relative trigger (`top 90%` / `top 10%`) was already ~28% into its range at load (1334px instead of 1280px), because there wasn't enough room above it for that viewport-relative math to resolve to zero. Fixed by switching the trigger to the same page-scroll-distance pattern used earlier in this project for an equivalent bug: `trigger: heroRef.current, start: "top top", end: "+=600"` — this ties progress to actual scroll distance instead of the panel's position relative to the viewport, guaranteeing a clean 0% at load regardless of hero height.
    - Verified end to end: `npm run build` passes clean; measured on the live page at a 1440x900 viewport — initial state 1280px (exact match with the heading), scrolled state 1440x900 (full bleed, square corners); zero console errors; screenshots confirm the visual result at both states.

26. **New background-removed photos wired in, fixed a live broken image (2026-08-10)** — **done.** Bilal supplied two new headshots with the background removed: `public/images/bilal-shafqat-coat.avif` (navy blazer, white shirt, pocket square) and `public/images/bilal-shirt.avif` (plain white shirt, more casual). Checking the codebase before making changes surfaced a real problem: the old photo files these components pointed to (`bilal-about.jpg`, `bilal-hero.jpg`) no longer exist in `public/images/` — confirmed via a direct 404 on the live dev server — meaning the About panel photo on the live homepage was broken before this fix, not just outdated.
    - **Live site**: `HeroAlt.tsx`'s About panel now uses `bilal-shafqat-coat.avif` — chosen over the shirt version because the blazer reads more consistent with the "senior partner" positioning in the headline above it. Also fixed `StructuredData.tsx`'s SEO/JSON-LD `image` field, which pointed at the same missing `bilal-hero.jpg`.
    - **Stale/unused files** (`Hero.tsx`, `HeroWordmarkTest.tsx`, `HeroYellowTest.tsx`, `About.tsx`) also had the same broken references — fixed all of them so nothing in the codebase points at a deleted file, using the coat photo for the two "About"-style slots and the shirt photo for the two original "hero"-style slots, giving the shirt photo an actual use rather than leaving it unreferenced.
    - Verified on the live homepage via Playwright: image resolves through Next's optimizer with zero failed requests and zero console errors, natural dimensions 1412x1186 confirmed, screenshot shows the transparent cutout blending cleanly against the gold panel background.
    - **Flag for Bilal**: only one photo slot exists on the current live design, so the shirt photo isn't visible anywhere on the actual homepage yet — it's only used in unused/preview files. If there's a specific second spot in mind for it (e.g. a future About page, testimonials, or swapping it into the hero itself), let me know.
27. **Hero (black top section) restructured to a centered layout with a large photo, About panel untouched (2026-08-10)** — **done.** Bilal shared a Behance profile page (Jonny Czar, Experience Designer at Hotjar) as a layout reference — big centered headline, a smaller centered subtitle line, then a large photo directly below, fading into the page background at the top. He was explicit the headline copy itself ("15 years of marketing, design, and development experience — in one senior partner") stays completely unchanged, and that only the hero section should change, not the yellow About panel below it.
    - `HeroAlt.tsx`'s `#home` section changed from the two-column 70/30 split (heading left, paragraph+CTAs right) to a single centered column: headline (untouched text), supporting paragraph, then CTAs, all centered, followed by a large photo beneath.
    - The photo uses `bilal-shirt.avif` (the coat version stays in the About panel directly below, so the same photo doesn't repeat back to back while scrolling) with a CSS `mask-image` gradient fading the top ~20% into transparent, so it blends into the black background the same way the reference photo fades into white — adapted to this site's dark theme rather than copying the reference's black-and-white treatment, since color plus the gold accent is the established brand look.
    - Verified: `npm run build` passes clean, zero console errors on the live page, screenshots confirm the composition at both desktop (1440px) and mobile (390px) widths. The yellow About section (`#about`) and its scroll-expand animation were not touched, confirmed still working.
    - **Reverted same day.** Bilal asked to revert immediately after seeing it live. `#home` is back to the 70/30 split (headline left, paragraph + CTAs right) exactly as it was before this item. The `bilal-shirt.avif` photo is no longer used anywhere on the live homepage as a result — it remains available in `public/images/` and wired into the unused/preview files from item 26 if a future placement comes up.
28. **About panel (`#about`) rebuilt as a centered layout with a glassmorphism panel, replacing the solid gold fill (2026-08-10)** — **done, hero/`#home` untouched.** Bilal asked for the same Behance-reference centered layout (badge → big headline → subtitle → large photo) applied specifically to the About panel this time (not the hero, which stays reverted per item 27), plus swapping the solid yellow background for a glass effect.
    - Content is unchanged (same headline, bio paragraph, and the three "what I do" items) but restructured: everything is centered instead of the previous two-column (text left, small photo right) layout, and the three bullet points render as a centered row of pill chips rather than a stacked list, echoing the reference's compact centered subtitle line while keeping all the original information.
    - The panel's fill changed from `bg-gold` to `glass-strong` — the same frosted-glass utility class already used on Contact's card (`rgba(18,18,24,0.72)` background, `backdrop-filter: blur(20px)`, subtle border) — so this reuses an existing, established style rather than inventing a new one. Added two blurred `.blob` glow shapes (gold + violet, the same technique Contact.tsx uses) behind the content so the glass has color to catch, rather than sitting flat.
    - Text/badge colors flipped from dark-on-gold (`#14140f`) to the site's standard light-on-dark tokens (`text-ink`, `text-muted`, gold badge), since the panel is no longer a light-colored surface.
    - The photo (`bilal-shafqat-coat.avif`) is now large and centered below the text, with the same top-edge fade-to-transparent mask technique used in item 27's (reverted) hero exploration, so it blends into the glass panel rather than sitting in a hard-edged box.
    - The panel's existing scroll-driven expansion (80vh → 100vh, corners squaring off) and its width-matches-the-heading behavior from items 24-25 are both untouched and still working — only the fill, text colors, and content layout changed, not the animation mechanics.
    - Verified: `npm run build` clean, zero console errors, zero failed requests. Screenshots confirm the glow blobs are visible through the translucent panel at both the initial (80vh, gold+violet glow visible on a dark card) and scrolled (full-bleed 100vh) states, and mobile (390px) stacks cleanly.
29. **About panel polish: hidden scrollbar, wider text, bigger bottom-anchored photo, switched to the shirt photo (2026-08-10)** — **done.** Follow-up refinements to item 28's centered/glass layout.
    - Added a `.no-scrollbar` utility to `globals.css` (`scrollbar-width: none` + `::-webkit-scrollbar { display: none }`) and applied it to the panel's scrollable inner container — content still scrolls internally if it overflows the panel height, the scrollbar itself just no longer shows.
    - Widened the headline, bio paragraph, and chip row from `max-w-2xl` to `max-w-3xl lg:max-w-4xl`, so text lines run noticeably wider on desktop instead of staying narrow in a tall glass panel.
    - Photo: switched from `bilal-shafqat-coat.avif` to `bilal-shirt.avif` as requested, sized up from `max-w-xs sm:max-w-sm` to `max-w-sm sm:max-w-md lg:max-w-lg`, and changed from a fixed-aspect box to a `flex-1` container with `object-contain object-bottom` so the photo now fills the remaining vertical space in the panel and sits flush against the bottom edge, rather than floating with a gap beneath it. The existing top-fade mask carried over so it still blends into the glass panel.
    - Verified: `npm run build` clean, zero console/network errors, confirmed `bilal-shirt.avif` is the image actually being requested, and confirmed the scrollable container has no overflow at the tested viewport. Screenshots checked at both panel states (80vh and full-bleed) and on mobile.
30. **Photo switched from object-fit to a true CSS background-image for zoom/crop control (2026-08-10)** — **done.** Bilal asked for `background-size: 15%` and `background-position: center 25%` specifically so the photo could zoom in and crop to fill the frame properly. Implemented as `background-size: 150%` — literal 15% would shrink the photo drastically, the opposite of "bigger and zoom," so read as a typo for 150%; flagged this substitution to Bilal so he can correct the exact number if he meant something else.
    - `next/image`'s `object-fit`/`object-position` don't support arbitrary zoom percentages the way CSS `background-size` does, so the photo element changed from a Next `<Image fill>` to a plain `div` with `backgroundImage: url(...)`, `backgroundSize: "150%"`, `backgroundPosition: "center 25%"`, `backgroundRepeat: "no-repeat"` — giving Bilal's exact requested controls directly. Kept the existing top-fade mask on the same element. Added `role="img"` + `aria-label` since a background-image div has no native alt text.
    - This trades away Next's automatic image optimization/responsive sizing for this one decorative photo (it now loads directly from `public/images/` at full file size, ~580KB) in exchange for literal background-size/position control — acceptable for a single decorative hero-panel photo, but worth knowing if Bilal later cares about shaving load time here specifically.
    - Removed the now-unused `next/image` import from `HeroAlt.tsx` since this was the file's only remaining usage of it.
    - Verified: `npm run build` clean (confirms no unused-import errors), zero console/network errors, screenshots confirm the photo is now visibly zoomed and cropped (filling the frame edge-to-edge) rather than the previous letterboxed/contained look, on both desktop and mobile.

31. **Hero headline/subhead copy revised for a senior-consultant tone, jargon removed (2026-08-11)** — **done.** Bilal had directly edited the headline to "15 years of Experience in Digital Marketing & Technical Growth, In One Senior Partner" with a subhead about "full-funnel growth" and "automated sales systems," then said it read cheap, like a freelancer listing services, rather than a consultant companies would trust as an agency alternative. Talked through why: stacking verbs ("design, build, and market") and defensive framing ("no account managers" as an excuse rather than a selling point) undercut authority. He picked the "Anchor" direction, keeping his original, already-approved headline as the benchmark rather than replacing it.
    - Headline reverted to the original wording: "15 years of marketing, design, and development experience, in one senior partner."
    - Subhead rewritten from an activity list to a trust statement: "Direct access to senior-level expertise, no account managers, no junior staff, and no handoffs between departments. Trusted by businesses across the UAE for paid marketing, web and app development, and design, delivered personally from strategy to launch." Keywords (UAE, paid marketing, web and app development, design) are placed here rather than in the headline itself, so the H1 stays clean and authoritative while the supporting text still carries the searchable terms for Google/Bing/AI indexing.
    - Verified: `npm run build` clean, zero console errors, screenshot confirms both lines render correctly with no spacing issues.
    - **Not yet actioned, discussed but pending a decision**: a 4-pillar services restructure (Paid Marketing & Lead Generation, Website & App Development, Design/Content & Conversion, CRM & Marketing Automation) with plain-language pillar titles, the original jargon-heavy bullet detail kept as-is since specific named platforms (HubSpot, Zoho, Salesforce, Next.js, Performance Max) are valuable exact-match SEO/GEO keywords, plus a one-sentence plain-English intro under each H3 before the bullets so AI chatbots have something quotable per section. Bilal hasn't confirmed whether to implement this yet.
32. **Automation folded into the hero subhead and About copy; About paragraph rewritten (2026-08-11)** — **done.** Bilal asked how adding "automation" would sound. Recommendation given: don't add a fourth noun to the tight three-item headline (dilutes the punch), but it fits naturally into the subhead and About text where a list already exists.
    - Hero subhead: "...paid marketing, web and app development, design, and automation, delivered personally from strategy to launch." Headline itself is untouched (still 3 items: marketing, design, development).
    - About panel paragraph rewritten from a first-person task list ("I run paid marketing campaigns, design and build... develop... produce...") to a comparison-framed statement: it now opens with what companies normally have to coordinate (agency + developer + freelancer) before saying he does all of it himself, "as one senior partner", matching the trust framing established in item 31 rather than reading as a checklist.
    - The `intersection` chip list (About panel) updated from 3 to 4 items, added "CRM & marketing automation", and softened "Web, mobile & MERN stack development" to "Web, mobile & app development" for a more scannable, plain-language chip, since "MERN stack" is still present elsewhere on the site (layout.tsx metadata, StructuredData.tsx, Services.tsx, Portfolio, WhoIWorkWith) for the specific keyword value, so nothing was actually lost, just not repeated in this one panel.
    - Verified: `npm run build` clean, zero console errors, confirmed exact rendered text for both paragraphs and all 4 chips, screenshot confirms the layout holds with the extra chip.

33. **Services section rebuilt around the four pillars; SEO/GEO metadata aligned sitewide (2026-08-12)** — **done.** Bilal asked whether the live Services section already matched the four-pillar plan discussed two days earlier. It didn't: the live section was still the older "Seven Services, One Point of Contact" structure (3 categories, 7 individual services, built 2026-08-03, before the four-pillar conversation happened), and it had no CRM/automation offering represented at all.
    - `Services.tsx` restructured: dropped the 3-category/7-service nesting entirely, pillars are now the top-level unit (4 items, not 7 across 3 groups). Each pillar uses the plain-language title agreed on previously (Paid Marketing & Lead Generation / Website & App Development / Design, Content & Conversion / CRM & Marketing Automation), a new one-sentence plain-English description added under each H3 so there's something quotable for AI summarization, and the original specific-keyword bullets (Google Search & Performance Max, HubSpot/Zoho/Salesforce, Conversions API, etc.) kept verbatim as capability chips, since those named platforms are valuable exact-match search terms. Section heading updated from "Seven Services" to "Four Pillars." New icons added for the restructure (Target, Workflow) alongside the existing Code2 and Palette; the now-unused Megaphone/TrendingUp/Globe/Smartphone/Share2 icon imports were removed.
    - Found and fixed a consistency gap while doing this: `StructuredData.tsx`'s schema.org `knowsAbout`/`makesOffer` list and `public/llms.txt`'s services section were both still describing the old 7-service structure, meaning the page content and the machine-readable data about that content had already drifted apart. Updated both to the four pillars plus key named-platform keywords, and updated `layout.tsx`'s meta description, keywords, and OpenGraph description the same way (added CRM/marketing automation keywords, removed the now-inaccurate "six disciplines" framing).
    - Verified: `npm run build` clean, zero console errors, confirmed via Playwright that all 4 pillar buttons render, are independently clickable, and each shows the correct title/description/capability chips (including the previously-missing CRM & Marketing Automation pillar). Screenshots confirm the visual result for pillar 1 and pillar 4.

34. **Dedicated Services pages built: hub + 4 pillar detail pages, FAQ schema, sitemap updated (2026-08-12)** — **done, round 1 of 2 (pillar pages now, granular sub-pages deferred).** Bilal asked for a proper Services page with each of the four pillars linking out to its own detail page, specifically for keyword ranking and so visitors can understand each service in depth, matching a hub-and-spoke ("silo") SEO structure he sketched out (e.g. Paid Marketing → Google Ads / Meta Ads / Performance Marketing). Given a scope choice (build everything at once vs. pillar pages first vs. hub only), he chose pillar pages first, with the ~12 granular sub-pages (Google Ads, Meta Ads, etc.) deferred to a second round once these are live and reviewed.
    - **New shared data source**: `src/data/pillars.ts` is now the single source of truth for all four pillars (icon, accent, label, short description, capability tags, a longer hero description, 3 detailed sections per pillar covering what would have been the sub-pages' topics, and 3 FAQs per pillar). `Services.tsx` (the homepage section) was refactored to import from this file instead of maintaining its own duplicate list, closing off the kind of drift that caused the earlier StructuredData/llms.txt mismatch (item 33).
    - **`/services`** (new hub page): H1 "Four pillars, one senior partner," a card grid of all four pillars linking to their detail pages, own metadata and canonical URL.
    - **`/services/[slug]`** (new dynamic route, statically generated for all 4 slugs via `generateStaticParams`): breadcrumb (Home / Services / [Pillar]), H1 = pillar name, hero description, CTAs, then each pillar's 3 sub-topics rendered as full H2 sections with body copy and capability tags (this is where "Google Ads," "Meta Ads," "Performance Marketing," etc. actually live for now, as sections within the pillar page rather than separate URLs, per the round-1 scope decision), a Frequently Asked Questions section (3 real Q&As per pillar, not placeholders), and links to the other 3 pillars.
    - **FAQPage + Service JSON-LD** added per pillar page, this closes an SEO gap flagged months ago in this project (`FAQPage` schema was deliberately skipped earlier because there was no real FAQ content, now there is). Verified via `curl` that both schema blocks are actually present in the rendered HTML, not just written in code.
    - **Homepage `Services.tsx`** now has a "Learn more about this pillar" link on the active preview card (pointing to `/services/[slug]`) and an "Explore all services in detail" link to the hub, so the interactive homepage section now funnels into the deeper pages rather than being a dead end.
    - **`sitemap.ts`** updated to include `/services` and all 4 pillar URLs, generated from the same shared `pillars` data (no manual list to keep in sync).
    - **Content honesty note**: section and FAQ copy is genuinely detailed (not filler), but written from what's already established about Bilal's actual services, tools, and positioning elsewhere on the site (Google/Meta/LinkedIn/TikTok, HubSpot/Zoho/Salesforce, Next.js/MERN), not fabricated case studies or invented numbers. No stats, testimonials, or client names were made up.
    - **Deliberately not done yet**: the ~12 granular sub-pages (e.g. `/services/paid-marketing/google-ads` as its own URL) per Bilal's chosen pacing. The pillar pages' existing sections cover the same topics for now; splitting them into separate URLs is the planned round 2 once these are reviewed.
    - Verified: `npm run build` clean (all 4 pillar pages confirmed statically generated), zero console errors, zero failed requests, confirmed via Playwright that the homepage's "Learn more" link, the hub page's 4 card links, and all 4 pillar pages' heading structure (1 H1, multiple H2/H3) all resolve correctly. Screenshots confirm the hub grid, a pillar detail page, and its FAQ section.

35. **Full-width mega menu on desktop nav, pillar content greatly expanded, real hover bug fixed, social links + footer overhaul (2026-08-12)** — **done.** A large multi-part request: a hover-triggered mega menu under "Services" in the nav, far more granular service data (specific ad platforms and design disciplines Bilal has actual portfolio evidence for), a real reported bug where the menu closed before a visitor could reach it with their mouse, all 9 of Bilal's real social profile URLs wired into the footer and Contact section, a footer text rewrite, and swapping in two new logo files.
    - **Mega menu**: `Nav.tsx`'s "Services" link is now a hover-triggered full-width panel (desktop only, `lg:` breakpoint, the mobile menu still shows a plain link). Four columns pulled directly from `pillars.ts`, each with an icon+label heading linking to `/services/[slug]` and a list of that pillar's specific services linking to `/services/[slug]#[section-slug]` for direct deep-linking to the right part of the page.
    - **Real bug found and fixed**: initial hover-based open/close (`onMouseEnter`/`onMouseLeave` on a small wrapper around just the trigger link) had a genuine geometric gap between the trigger and the panel below it (the panel is positioned relative to the whole fixed header, which is taller than the trigger link's own box) — moving the mouse from the link down into the menu passed through dead space that belonged to neither element, closing the menu before a visitor could reach it. Confirmed exactly matches Bilal's report. Fixed with a 250ms close-delay (`setTimeout`, cancelled on re-entry) rather than fragile pixel-perfect gap-matching, the standard robust pattern for this exact class of mega-menu bug. Verified by simulating a genuine step-by-step mouse path in Playwright (not an instant `.hover()` jump, which is why the first "verified working" pass hadn't caught it) — confirmed the menu now survives real mouse travel and a sub-link click lands on the correct page and anchor.
    - **Legibility fix along the way**: the mega menu panel initially used the existing `glass-strong` class (72% opacity), which let the busy hero headline behind it visually fight with the menu's own text. Added a new `.glass-nav` utility (97% opacity, more blur) specifically for panels sitting directly over dense page content, used here rather than making `glass-strong` itself more opaque everywhere it's already used.
    - **Pillar content greatly expanded**: Bilal provided a list of specific services he has real portfolio evidence for (sales/gamification graphics, real estate ad carousels, mobile app UI, email templates, full brand guideline decks) that weren't represented as their own topics yet. Pillar 1 (Paid Marketing) grew from 3 to 9 sections: Google Ads, Meta Ads, TikTok Ads, Snapchat Ads, LinkedIn Advertising, Demand Generation, Email Marketing, WhatsApp Marketing, and Performance Marketing & Optimisation. Pillar 3 (Design, Content & Conversion) grew from 3 to 11: UI/UX Design, Web Design, Mobile App Design, Branding, Brand Guidelines, Business Profile, Social Media Strategy & Design, Social Media Marketing, Social Media Planning, B2B & Real Estate Video Editing, and CRO. Each new item is a genuine H2 section with real body copy and capability tags, not just a menu label, so every mega-menu link actually lands somewhere substantive (confirmed via Playwright: 12 H2s on the Paid Marketing page, 14 on Design/Content/Conversion, zero duplicate anchor IDs). This is exactly the kind of content depth the whole services-page effort (item 34) was for.
    - **Social links, real URLs**: created a shared `SocialLinks.tsx` component (icons + the `socials` array in one place, used by both `Footer.tsx` and `Contact.tsx`, avoiding yet another duplicate-list drift risk) covering all 9 platforms Bilal provided real URLs for: Facebook, X, LinkedIn, Instagram, TikTok, YouTube, Pinterest, Behance, and Dribbble (the previous footer only had 4, all pointing at `#` placeholders). X, TikTok, YouTube, Pinterest, and Dribbble got new custom inline SVG glyphs (lucide doesn't include brand-specific icons for these, matching the existing approach used for the Behance "Be" monogram). Added the same row to the Contact section, which had no social links at all before.
    - **Footer text rewrite**: "Paid Marketing, Design & Development Studio" / "Paid marketing, web & mobile development, and design — under one roof." replaced with "Paid Marketing, Development & Design Partner" / "One senior partner for paid marketing, website & app development, design, and the CRM automation that connects them.", matching the senior-partner positioning established earlier in items 31-32 rather than the older generic "studio" framing.
    - **New logo files wired in**: Bilal supplied `bilal-square-dark.svg` and `bilal-square-light.svg` (a stacked icon+wordmark lockup, not yet used anywhere). Determined which was which by rendering both on white and black test backgrounds rather than guessing from filenames alone, since the naming was genuinely ambiguous — "dark" turned out to mean the dark-coloured mark (for light backgrounds) and "light" the white-coloured mark (for dark backgrounds). Used `bilal-square-light.svg` in the footer, which sits on the site's dark background.
    - Verified end to end: `npm run build` clean, zero console errors on any page touched, all 9 footer/Contact social links confirmed pointing at the correct real URLs, expanded pillar pages confirmed rendering with no duplicate IDs, mega menu confirmed to survive realistic mouse movement.
36. **Mega menu reorganized into 8 balanced sub-groups instead of 4 dense pillar columns (2026-08-12)** — **done.** Bilal felt the 4-column mega menu from item 35 looked too dense (11 items stacked in one column) and proposed expanding to 8 top-level categories, including "Digital Marketing" and "Performance Marketing" as separate siblings. Flagged a real problem with that specific split before building it: performance marketing is a discipline *within* digital marketing, not a parallel category, making them siblings would read as overlapping/confusing to visitors and dilute topical clarity for Google/AI indexing (similar issue with "Web & Mobile App Development" vs. the proposed "Complete Performance Application Development", likely overlapping). Proposed an alternative that solves the actual density problem without the overlap risk or the cost of a full 8-page rebuild: keep the 4 pillar pages/URLs/schema exactly as built in item 35, but reorganize only the mega menu's visual layout into 8 shorter sub-groups by splitting the two dense pillars in half. Bilal approved this direction.
    - Added `megaMenuGroups` to `pillars.ts`: 8 curated groups (Paid Advertising, Lead Generation & Outreach, Website & App Development, UI/UX & Product Design, Branding, Social & Content, Video & Conversion, CRM & Automation), each referencing a `pillarSlug` and a list of item titles that must exactly match a real section title on that pillar's page (so the link, built from `slugify(item)`, lands on a real anchor, not a dead link). Validated this programmatically (via a one-off `tsx` script cross-checking every item against actual section titles) rather than just eyeballing it, all 20 items matched.
    - `Nav.tsx`'s mega menu now maps over `megaMenuGroups` instead of `pillars`: a `grid-cols-4` grid auto-wraps the 8 groups into 2 rows of 4, each group shown as a small coloured dot (matching its parent pillar's accent, gold/violet/cyan) plus a bold title and its 2-5 items underneath, replacing the previous large icon-badge treatment (which suited 4 big categories, not 8 smaller ones).
    - The underlying pillar pages, their content, sitemap entries, and structured data are all completely unchanged, this was purely a navigation-layer reorganization.
    - Verified: `npm run build` clean, zero console errors, confirmed via Playwright that all 8 group titles render in the correct order with 26 correctly-linked items total, and re-ran the realistic mouse-travel test from item 35 against the new layout including the farthest corner item, menu still survives the travel and the click correctly lands on `/services/crm-marketing-automation#marketing-automation-workflows`. Screenshot confirms the panel is visually shorter than the previous 4-column version despite showing twice as many categories.

37. **Mega menu corrected to a true 4-main + nested-sub-categories hierarchy (2026-08-12)** — **done.** Bilal clarified item 36's 8-group version wasn't quite what he meant: he wanted the 4 main pillars to stay visible as the primary structure, with sub-categories nested underneath them, not 8 flat sibling columns.
    - `Nav.tsx`'s mega menu now iterates the 4 `pillars` as the outer structure (icon + pillar name as a real header again, like the original item 35 version), and for each pillar looks up its `megaMenuGroups` entries: if a pillar has only one group (Website & App Development, CRM & Marketing Automation), its items render directly under the pillar header with no redundant extra label. If a pillar has multiple groups (Paid Marketing → Paid Advertising + Lead Generation & Outreach; Design/Content/Conversion → 4 groups), each renders as a small uppercase sub-heading nested under that pillar's header.
    - Outer grid changed from equal `grid-cols-4` to `grid-cols-[1fr_1fr_2fr_1fr]`, giving the Design pillar's column double width so its 4 sub-groups can sit in an internal 2×2 grid instead of stacking 4-deep and getting tall again; the other 3 pillars (1 or 2 groups each) stay single-width.
    - Also answered a direct question in the same message: a table comparing Digital Marketing, Demand Generation, Lead Generation, and Performance Marketing, clarifying that Digital Marketing is the umbrella term and the other three are a discipline/tactic/methodology within it, not parallel siblings, which is the exact reasoning behind flagging his original 8-category proposal in item 36.
    - Verified: `npm run build` clean, zero console errors, confirmed all 26 items still present and correctly linked, re-ran the realistic mouse-travel test (including to the farthest item) and the menu still survives it. Screenshot confirms the 4-pillar-header + nested-sub-group hierarchy renders as intended.

38. **Mega menu rebuilt as 8 standalone discipline columns (2×4), replacing the 4-pillar-nested version (2026-08-12)** — **done.** After seeing the actual live nested version from item 37 (not the stale screenshot he'd sent, confirmed via a fresh no-cache browser check that the nested hierarchy was genuinely already live), Bilal asked for something different again: 8 fully separate columns by discipline rather than sub-groups nested under the 4 pillar headers. Named boundaries: Paid Marketing on its own, social/digital marketing on its own, web & app dev on its own, CRM integration folding in MarTech and Performance Marketing "to show data organized and reusable," UI/UX separate, and graphic design separate.
    - Reviewed and confirmed the exact 8-column split before building (per the established pattern in this project of proposing a table for approval on real IA decisions): Paid Marketing, Social Media Marketing, Digital Marketing & Outreach, Website & App Development, UI/UX Design, Graphic Design & Branding, CRM & MarTech Integration, Video & Conversion. All 26 existing items accounted for, none dropped, none duplicated.
    - **Data model change**: `MegaMenuGroup.items` changed from `string[]` to `{ title, pillarSlug }[]`, since "CRM & MarTech Integration" is the first group to deliberately mix items from two different underlying pillars, it includes "Performance Marketing & Optimisation," whose actual content still lives on the Paid Marketing pillar page. Previously `pillarSlug` was set once per group; now it's per item, so that link still correctly points to `/services/paid-marketing#performance-marketing-optimisation` rather than a broken URL on the CRM pillar. Validated all 26 (title, pillarSlug) pairs programmatically against real section titles before touching `Nav.tsx`, not just visually.
    - `Nav.tsx` reverted from the item-37 nested-under-4-pillars layout back to a flat `grid-cols-4` grid (auto-wrapping to 2 rows for 8 items), each column showing a coloured dot + bold title + its items, no pillar icon header this time since there are 8 standalone columns now, not 4.
    - **Infrastructure note**: the dev server that had been running throughout this whole session (not started by any of these changes) had stopped by the time this was verified, unrelated to the code edit. Restarted it to complete verification.
    - Verified with a fresh, cache-busted browser context: all 8 group titles render in the intended order, all 26 items present, the cross-pillar Performance Marketing link resolves correctly, and the menu survives the realistic mouse-travel test out to the farthest item. `npm run build` clean, zero console errors.

39. **"Growth Marketing" and "Lead Generation" added as real sections, not just menu labels (2026-08-12)** — **done.** Prompted by comparing a competitor's resume (Md Arman Hossean, a Dubai-based Performance Marketing Manager) whose "Areas of Expertise" listed Growth Marketing, Lead Generation, and Demand Generation cleanly. Checked the site first rather than assuming: Demand Generation and Digital Marketing already existed, but Growth Marketing didn't exist anywhere, and Lead Generation only lived buried inside a capability chip's text ("Lead Generation & Demand Generation"), not as its own clickable section anywhere.
    - Recommended against giving either one its own top-level mega-menu column: Growth Marketing is a cross-funnel methodology that overlaps with Paid Marketing/Digital Marketing & Outreach/CRM & MarTech combined, and Lead Generation overlaps with Demand Generation and Performance Marketing. Adding either as a 9th/10th sibling column would reintroduce the exact overlap problem already fixed twice in items 36-38.
    - Instead added both as genuine new H2 sections with real, non-redundant body copy, not decorative labels: **Lead Generation** (capturing contacts via landing pages/forms/gated content, distinct from Demand Generation's awareness-building and Performance Marketing's spend optimisation) added to the Paid Marketing pillar, slotted into the "Digital Marketing & Outreach" mega-menu column right after Demand Generation, forming a natural funnel: awareness → capture → nurture. **Growth Marketing** (cross-channel, full-funnel experimentation spanning acquisition/conversion/retention) added to the CRM & Marketing Automation pillar, since that pillar's own framing is literally "connects everything else into one system," and appended to the "CRM & MarTech Integration" mega-menu column.
    - Validated all 28 (title, pillarSlug) pairs programmatically (28 now, up from 26) before touching any UI code, then separately verified both new links resolve to real rendered H2 sections with real body text on the live pages, not just checking the data file.
    - Verified: `npm run build` clean, zero console errors, screenshot confirms both new items appear in their intended columns without disrupting the existing 8-column layout from item 38.

40. **Keyword strategy for winning freelance work — full tier list (2026-08-13)** — **researched, build pending.** Bilal asked for keyword rankings mapped to his actual skills, specifically to win freelance clients. Built from the real 28 sections in `src/data/pillars.ts`, not from a generic keyword list.
    - **No search volume figures are recorded here on purpose.** Accurate Dubai-specific volumes cannot be produced without pulling Google Keyword Planner, and inventing them would be worse than useless. Bilal already runs Google Ads so Planner access is free to him. Tiers below are therefore ranked by **buyer intent × realistic winnability**, which is the metric that actually decides revenue for a solo freelancer, rather than by raw volume.
    - **The real bottleneck is pages, not keywords.** 28 services currently sit on 6 indexable pages (`/`, `/services`, 4 pillar pages, `/portfolio`). Google ranks pages, not `#fragments`, so a mega-menu link to `/services/design-content-conversion#ui-ux-design` can never rank for "freelance UI UX designer Dubai". Most of the tier-1 list below is blocked on page creation, not on copy tweaks.

    **Tier 1 — target first. High hire intent; the "freelance" modifier filters out agencies Bilal cannot yet outrank.**
    | Keyword | Why it wins | Target page |
    |---|---|---|
    | freelance digital marketer Dubai | Single best commercial term for his positioning | Homepage |
    | freelance web developer Dubai | Huge hire intent, freelancer-qualified | needs building |
    | freelance Google Ads specialist Dubai | Buyer already knows what they want | needs building |
    | freelance UI UX designer Dubai | Lower competition than "web designer" | needs building |
    | freelance graphic designer Dubai | High volume, weak competitors | needs building |
    | freelance mobile app developer Dubai | Expensive service, few real freelancers | needs building |
    | Next.js developer Dubai | Very low competition, he genuinely qualifies | needs building |
    | freelance HubSpot consultant UAE | Small volume, very high value per lead | needs building |

    **Tier 2 — the unfair advantage.** Low volume, near-zero competition, high conversion because the searcher has a specific technical problem and no idea who solves it: `Meta Conversions API setup Dubai`, `server-side tracking setup UAE`, `server side GTM consultant Dubai`, `Zoho CRM setup Dubai`, `HubSpot Meta Ads integration`, `MERN stack developer Dubai`, `WhatsApp marketing Dubai`, `conversion rate optimisation freelancer Dubai`. Accessibility/WCAG terms belong in this tier too — listed in full under Phase 3 item 13. **Server-side tracking is the standout**: most Dubai marketers cannot implement it, most developers don't understand why it matters, and Bilal does both. Genuinely defensible.

    **Tier 3 — the real estate vertical (strongest unplayed card).** Property is Dubai's largest ad-spend category and Bilal has real developer-side experience: `real estate lead generation Dubai`, `real estate marketing freelancer Dubai`, `off plan property lead generation`, `property developer marketing Dubai`, `real estate video editing Dubai`, `Dubai real estate Google Ads`. Note the `B2B & Real Estate Video Editing` section is currently buried inside the design pillar and deserves its own page.

    **Tier 4 — AI search works on questions, not keywords.** ChatGPT/Claude/Perplexity match phrasing and cite whoever answers cleanly: "who can build a website and run the ads for it in Dubai", "freelance alternative to a marketing agency in Dubai", "how much does a freelance web developer charge in Dubai", "do I need an agency or a freelancer for Google Ads". The existing `FAQPage` schema on pillar pages is the right mechanism already; it just needs more entries phrased as questions people ask out loud.

    **Tier 5 — deliberately skip.** Do not chase `digital marketing agency Dubai`, `SEO company Dubai`, `web design Dubai`, `PPC Dubai`. Established agencies own these on years of backlinks; a year of effort would land page 4. Revisit only after domain authority builds.

    - **Pages to build, priority order:** (1) `/contact` — currently only a `#contact` anchor, so there is no local ranking signal and nothing for a Maps listing to point at; (2) 8 standalone tier-1 service pages: `google-ads`, `meta-ads`, `website-development`, `mobile-app-development`, `ui-ux-design`, `graphic-design`, `crm-setup`, `server-side-tracking` — content for all 8 already exists in `pillars.ts`, so this is mostly restructure, not new writing; (3) `/about` — same anchor problem, and this is the page that ranks for his name and feeds AI systems his credentials; (4) `/real-estate-marketing` vertical page; (5) `/pricing` — "how much does X cost Dubai" is a large query family and almost no Dubai freelancer publishes numbers.
    - **Three technical gaps found while auditing (all still open):**
      - `StructuredData.tsx` lists **only Behance** in `sameAs`, while `SocialLinks.tsx` holds 9 real profile URLs. `sameAs` is the primary mechanism Google and AI models use to confirm one real person across the web. Adding the other 8 is a five-minute change with real entity-recognition value.
      - **No Google Business Profile signal.** Maps ranking comes almost entirely from a verified Google Business Profile, not from the website. The `Person` schema has no `telephone` and no `LocalBusiness` type, so nothing links the site to a listing. Set up the profile as a service-area business.
      - **Three preview routes are crawlable.** `/preview-hero-alt`, `/preview-hero-wordmark`, `/preview-hero-yellow` are excluded from `sitemap.ts` but `robots.txt` is `Allow: /`. They need `noindex` or they risk duplicate-homepage content.
    - **Off-site, same wording.** Use the identical tier-1 phrasing in the LinkedIn headline, Upwork and Fiverr profile titles, and Behance description. Those platforms rank in Google for these terms far faster than a new personal domain, and consistent wording across all of them reinforces the entity.

41. **Entity `sameAs` completed and the 3 preview routes de-indexed (2026-08-18)** — **done.** Two of the quick technical fixes flagged in item 40, both verified in the actually-rendered HTML rather than only in source.
    - **`sameAs` went from 1 URL to 9.** `StructuredData.tsx` listed only Behance while `SocialLinks.tsx` had held all 9 real profile URLs since item 35, so the schema had been silently out of sync with the footer for six days. All 9 are now in the `Person` schema, ordered by entity-verification value (LinkedIn and Behance first, since those are the profiles Google most readily reconciles against a professional identity). This matters specifically for the AI-search goal in item 40's tier 4: `sameAs` is the primary mechanism by which Google and LLM-backed search confirm that a set of scattered profiles is one real person, which is what lets a solo freelancer register as an entity at all rather than as an unverified name on a page.
    - **`noindex, nofollow` added to `/preview-hero-alt`, `/preview-hero-wordmark`, `/preview-hero-yellow`.** These are internal design-comparison routes left over from items 20-24. They were excluded from `sitemap.ts` but `robots.txt` is `Allow: /`, and sitemap exclusion is not a crawl directive, so all three were fully indexable. `/preview-hero-alt` was the real risk: it renders both the old `Hero` and the live `HeroAlt` on one page, so it duplicated the homepage's H1 and hero copy. Each page now carries a `robots` metadata export plus a comment explaining why, so a future session doesn't strip it as dead config.
    - Deliberately not bundled in: `telephone` on the `Person` schema and the Google Business Profile setup (both still open from item 40). The GBP listing is the part that actually drives Maps ranking and it can't be done from the codebase, so adding `telephone` alone would not have produced the Maps result and was left with that item rather than split off from it.
    - Verified: `npm run build` clean at 15 pages, and confirmed against the prerendered HTML that all 3 preview pages emit `<meta name="robots" content="noindex, nofollow">`, that the homepage does **not** (i.e. the change didn't leak into a real page), and that the homepage JSON-LD now carries exactly 9 `sameAs` URLs.

42. **`/about` and `/contact` built as real pages, closing the anchor-only gap (2026-08-18)** — **done.** The first two pages from item 40's build list. Both had existed only as `#about` / `#contact` fragments on the homepage since the project started, which is why neither could rank: Google indexes pages, not fragments, so there was no URL to target with a keyword and nothing for a future Google Business Profile listing to point at.
    - **`/about`**: H1 keeps the already-approved anchor line from item 31 ("15 years of marketing, design, and development experience, in one senior partner") rather than inventing a new headline for a second page. Sections: lead paragraph reusing the agency-vs-developer-vs-freelancer comparison framing from item 32, the four pillars **imported from `pillars.ts`** (not retyped, so this page cannot drift from `/services` the way `StructuredData.tsx` and `llms.txt` drifted in item 33), a "how working with me is different" trio, and a named platforms/tools grid. That last grid is deliberately keyword-dense with exact product names (Google Performance Max, Meta, TikTok, Snapchat, LinkedIn, Next.js, React Native, MERN, HubSpot, Zoho, Salesforce, Conversions API) since those are the exact-match terms identified in item 40's tier 2, and they double as the fastest way for a visitor to check fit. Uses `bilal-shafqat-coat.avif`, which had been sitting unused on live pages since item 29 swapped the About panel to the shirt photo.
    - **`/contact`**: H1 "Talk to the person who'll actually do the work". Three channel cards (email, WhatsApp, call, with the second phone number surfaced as a note), a "where I am and when" card, and a numbered "what to include in your first message" list. Carries `ContactPage` + nested `Person` JSON-LD with `email`, both `telephone` numbers, `PostalAddress`, and two `ContactPoint` entries — this is the schema a Google Business Profile listing will need to reconcile against, so it partly de-risks that still-open item from item 40.
    - **Closed a queued suggestion as a side effect**: suggestion-queue item 3 (time zone transparency) is now live on `/contact` as "Gulf Standard Time (GST, UTC+4), Monday to Friday" with an expected reply window. **Flag for Bilal**: "a reply typically within one business day, same day on WhatsApp" is a commitment about his own behaviour, taken from the wording the suggestion queue itself proposed. Confirm or change it, since it's a promise being made on his behalf.
    - **Deliberately no inline contact form.** `/api/lead` still has no CRM endpoint configured (there is no `.env.local`, only the example file), so a form on this page would accept submissions and silently drop them, which is worse than no form. The existing `LeadFormPopup` and WhatsApp chat widget already give visitors a form path sitewide. Add the inline form once the CRM credentials land.
    - **Also did not assume facts not already on the site**: `availableLanguage` in the schema was initially drafted as English + Urdu and cut back to English only, since nothing in the repo establishes which languages Bilal works in and inferring it from his name would be a guess. Worth adding properly if he confirms.
    - **Rewiring**: `Nav.tsx`'s "About" link now goes to `/about` (was `/#about`), and its desktop + mobile "Book a free consultation" CTAs now go to `/contact` (was `/#contact`, which bounced visitors on `/services` back to the homepage). `Footer.tsx`'s About/Contact quick links and the pillar pages' mid-page CTA updated the same way. `sitemap.ts` now emits both new URLs. The homepage's own in-page `#contact` anchors (HeroAlt, Engagement, WhoIWorkWith) were left as anchors on purpose, since smooth-scrolling to a section on the page you're already on is the correct behaviour there.
    - Verified: `npx tsc --noEmit` and `npm run build` both clean at 17 pages, both new routes statically prerendered. Confirmed against the prerendered HTML that each page has exactly **1 `<h1>`** (the bug that hit `/portfolio` earlier in this project), correct unique `<title>` and canonical, no stray `noindex` leaking from item 41, `ContactPage` schema present with both phone numbers, and all 9 sitemap URLs emitting including `/about` and `/contact`.

43. **Mega menu rebuilt on a GSAP timeline with a real blurred backdrop, plus a sitewide glassmorphism bug fixed (2026-08-20)** — **done.** Bilal asked for the mega menu to open with a proper GSAP animation rather than just appearing, and for it to have a genuine blurred background. Both were real gaps, and chasing the blur uncovered a pre-existing bug affecting every glass surface on the site.
    - **Why it could only ever "appear" before**: the panel was conditionally mounted (`{servicesOpen ? <panel/> : null}`), so React removed it from the DOM on close. Nothing can animate out of existence, so an exit animation was structurally impossible regardless of library. Restructured to keep the panel permanently mounted, hidden at first paint via inline `visibility: hidden; opacity: 0` (so it cannot flash before JS runs) and driven by a single paused `gsap.timeline()` built once on mount — `tl.play()` to open, `tl.reverse()` to close. GSAP's `autoAlpha` toggles `visibility` alongside opacity, which also keeps the 28 links out of the tab order and the accessibility tree while closed.
    - **The animation**: the panel unrolls downward via `clip-path: inset(0% 0% 100% 0%)` → `inset(0% 0% 0% 0%)` (chosen over a `scaleY` so the text inside is never distorted mid-animation) with a small `y: -12` settle, the scrim fades in underneath, and the 8 discipline columns stagger in at 0.045s intervals, with the "View all services" footer row last. Reduced-motion visitors get a 0.15s cross-fade with no travel and no clip reveal — verified separately with Playwright's `reducedMotion: 'reduce'`, which showed `clipPath: none` and `transform: none` with the panel already open at 200ms.
    - **The blur, and the real bug behind it**: `.glass-nav` was `rgba(10,10,13,0.97)`. At 97% opacity its own `backdrop-filter` had essentially nothing visible left to blur, which is why the effect never read. Lowering the opacity alone made it worse, and a screenshot showed the hero headline reading straight through the panel. Investigating that surfaced the actual cause: **globals.css hand-wrote `-webkit-backdrop-filter` immediately after the standard `backdrop-filter`, and Lightning CSS (which Tailwind v4 uses) collapsed the pair down to the `-webkit-` form only.** Confirmed by grepping the built CSS bundle, not by guessing. Since Firefox supports the standard property and ignores the `-webkit-` alias, **no glass surface on this site had ever rendered its blur in Firefox** — `.glass`, `.glass-strong`, the nav pill, the Contact card, and the About panel included. Fixed by deleting all 4 hand-written `-webkit-` declarations and letting Lightning CSS do the prefixing, plus adding an explicit `browserslist` to `package.json` (chrome 111 / safari 16.4 / firefox 121 / edge 111). The bundle now emits both the prefixed and standard forms for all four classes.
    - **Legibility is deliberately not dependent on the blur.** Final values are `.glass-nav` at 0.90 and a new `.mega-backdrop` full-viewport scrim at 0.82 with `blur(18px)`, chosen so the panel stays readable even where `backdrop-filter` does not render at all. The blur is an enhancement on top, never the thing legibility rests on. A comment in `globals.css` records this so a future session doesn't tune the opacity back down. The scrim sits at `-z-10` inside the `z-50` header: a `fixed` element would otherwise paint over the statically-positioned nav bar regardless of DOM order.
    - **A measurement mistake worth recording**: the first three verification passes reported `backdropFilter: "none"` and led to a wrong conclusion that the blur was being stripped entirely. The query read the unprefixed camelCase property, which returns `none` when only the `-webkit-` form is set. The screenshot was what actually settled it. Reading computed styles for a prefixed property needs `getPropertyValue('-webkit-backdrop-filter')`, or better, fixing the CSS so the standard property exists.
    - Verified against a **production** build (not just dev, since Lightning CSS behaves differently there): `tsc --noEmit` clean, `npm run build` clean at 17 pages, no new lint errors (the 2 in `Nav.tsx` are the pre-existing `<a>`-vs-`<Link>` ones from Phase 0 item 3), zero console errors. Sampled the opening frames to prove real animation rather than an instant appear (clip 41% → 12% → 1.5% → 0%, columns 0 → 0.49 → 0.9 → 1) and sampled mid-close to prove it animates **out** as well. Confirmed all 28 deep links still reachable and clickable, computed blur now present on both panel and scrim, no horizontal overflow, and the panel correctly stays hidden at 390px where the mega menu is desktop-only.

44. **First real case study live: `/portfolio/leos-developments` (2026-08-26)** — **done.** Bilal supplied the first real client assets, into `public/portfolio/leos/` (logo, corporate website screenshot, 7 social creatives). Built the client-level case study page agreed in the same session, so the structure is now proven against real material rather than described in the abstract.
    - **Looked at the assets before building anything**, which changed the plan: the `landing-page` file is not a campaign landing page, it is the **LEOS corporate website** (1928×6555 full-page capture), and the social creatives are **brand-level** (an Arabian Property Awards win, a "coming soon" teaser) rather than project-specific ad sets. So this is client-level material, which fits the client-page structure exactly — it is not a Hadley Heights project case study. Worth knowing before the remaining projects arrive.
    - **Page content is only what could be verified.** The intro facts (UK headquarters in Weybridge, Surrey; London offices; Dubai experience centre) were read off LEOS's own site in the screenshot, not invented. **No objective, no results, and no metrics are on the page**, because Bilal has not supplied them yet and fabricating them was not an option. The page currently proves capability visually; it does not yet tell a story. Filling `project.md` is what upgrades it from a gallery to a case study.
    - **Structure**: breadcrumb, white LEOS logo, H1, factual intro, a 4-item fact strip (client / sector / markets / scope), then the website shown inside a scrollable browser-chrome frame (`max-h-[70vh]`, which is how a 6555px-tall capture is shown without dominating the page), then the social grid, then the CTA. All images go through `next/image` with explicit `sizes`, so the 3.3MB website capture is resized per viewport rather than shipped whole — deliberately avoiding the raw-`background-image` trade made in item 30.
    - **Logo wall is now real.** `LogoWall.tsx` gained an optional `src`/`href` per entry: entries with a logo render as a clickable, grayscale-to-colour mark linking to that client's case study, entries without stay as text placeholders. LEOS is the first real one; 4 placeholders remain. This delivers the logo-wall-as-entry-point from Phase 3 item 14.
    - **Asset convention settled**: web-ready images live in `public/portfolio/<client>/<type>/`; `_incoming/` stays for raw material (PDFs, uncompressed exports). `_incoming/README.md` updated so the repo doesn't carry two competing conventions. Also added `.DS_Store` to `.gitignore` and removed the 4 that had appeared under `public/`.
    - **Permission: settled 2026-08-26.** Bilal worked with LEOS for two years and the same work is already public on his Behance profile. His decision is that portfolio display does not require separate sign-off, which is standard practice for design and marketing portfolios. Recorded so it is not re-raised. The only thing that would change it is his own contract or NDA terms, which are his to know. **This supersedes the per-brand permission check in Phase 3 item 14 for LEOS specifically**; still worth confirming for Refine, since that is a current employer rather than a past one.
    - **Still required**: the objective / what-I-did / results text. Sitemap entry added.
    - Verified: `tsc --noEmit` and `npm run build` clean at 18 pages, exactly 1 `<h1>`, all 11 images load with **0** broken requests, zero console errors, and the homepage logo wall confirmed linking through to the case study with the logo rendering.

45. **SEO/GEO pass on the LEOS case study, and AVIF enabled sitewide (2026-08-26)** — **done.** Audited the page built in item 44 against both classic SEO and AI-search extraction, then fixed what was actually missing rather than assuming.
    - **Found by auditing the rendered HTML**: no `og:image` at all (so LinkedIn and WhatsApp shares would have had no preview), no page-level structured data (only the global `Person`/`Service` from `layout.tsx`), no `BreadcrumbList` despite a visual breadcrumb existing, seven generic image alts ("social media creative 3"), the page orphaned from `/portfolio` itself, absent from `llms.txt`, and a 75-character title that would truncate in search results.
    - **Image alt text and captions rewritten from the assets.** Built a browser contact sheet of all 7 creatives and looked at them rather than guessing, then wrote alt text describing what each actually shows (the Arabian Property Awards 2023–2024 win over a high-rise exterior, the "Coming Soon" terrace teaser, the experience centre interior). This surfaced a genuinely useful fact: two of the creatives are branded **Hadley Heights** and **Weybridge Gardens**, so the page now carries real project names as verifiable content rather than only client-level copy. Each image also gained a visible `<figcaption>` — captions are extractable text, which is what AI search actually quotes; a gallery of unlabelled images is invisible to it.
    - **Structured data added**: `CreativeWork` (chosen over `Article` since this is a work sample, not editorial) with `creator` as the Person, `about` naming LEOS as an `Organization` so the page connects to that entity, plus `keywords` and `image`. Separate `BreadcrumbList` matching the visual trail. Both confirmed to parse as valid JSON in the rendered output.
    - **A "Scope of work" section added ahead of the galleries**, because the page was previously ~all images and AI search cites specific prose, not pictures. Three factual blocks: corporate website build, brand social creative, project campaign creative. Everything in it is verifiable from the assets or from what Bilal stated — **no objectives, results, or metrics were invented**, and that gap remains open.
    - **OG image is a JPEG, deliberately.** Generated a 1200×630 JPEG from the award creative rather than pointing at the AVIF source, because LinkedIn and WhatsApp render AVIF previews unreliably and a missing preview kills click-through on exactly the channels a portfolio piece gets shared on.
    - **Orphan fixed**: `/portfolio` now carries a featured-case-study link at the top, so the page is reachable from the section it belongs to and not only from the homepage logo wall.
    - **AVIF enabled sitewide** in `next.config.ts`. Investigating Bilal's question about `next/image` revealed Next's default `images.formats` is `["image/webp"]` only, so **his hand-converted AVIF sources were being re-encoded down to WebP**. Measured on his own files: the website capture went 684 KB → 613 KB and the social creatives 24–76 KB → 22–75 KB. Modest but free, and it means the AVIF conversion work is now actually honoured.
    - **Measured what `next/image` is worth on real assets**, since Bilal asked: 4,981 KB of source files are delivered as **463 KB on a 390px phone** (91% less) and **958 KB on a 1440px desktop** (79% less), the 1928×6555 website capture being served at 640px wide on mobile, with 2 of 11 images correctly deferred until scroll. Recorded because it settles the "is next/image worth it when I already converted to AVIF" question: resizing per device and file-format compression are different savings and they stack.
    - **A second measurement mistake worth recording** (same class as item 43's): an initial check reported the images were served as `image/jpeg`. That reading came from a manual `fetch()` inside the page, which sends `Accept: */*` and makes Next fall back to JPEG. Reading the real `<img>` responses' headers gave the true answer. Prefer observing actual resource responses over re-requesting an asset by hand.
    - Verified: `tsc --noEmit` and `npm run build` clean at 18 pages, 1 `<h1>` / 4 `<h2>` / 3 `<h3>`, all 3 JSON-LD blocks parsing, 7 figcaptions present, **0** generic alts remaining, title down to 58 characters, `og:image` and `summary_large_image` present, and the page linked from both `/portfolio` and `llms.txt`.

46. **Hadley Heights added as a project section inside the LEOS case study (2026-08-26)** — **done.** Bilal supplied `public/portfolio/leos/hadley-heights/` (project logo, a 1920×6682 landing page capture, and 5 carousel slides). Built as a **section within the client page** rather than its own route, per the structure agreed earlier the same day: client pages grow in depth, not count, so seven future projects do not become seven thin pages.
    - **Read the assets before writing, which produced real content instead of filler.** The landing page and carousel carry hard specifics: Jumeirah Village Circle, Dubai; 216 apartments and retail outlets; 1 and 2 bedroom; from AED 1 million; 1-bed 840.88–970.80 sq ft, 2-bed 1,284.57–1,425.03 sq ft; designed and developed in the UK by British architects. All of that is now extractable page text rather than trapped inside images, which is what makes it usable by Google and quotable by AI search.
    - **Described the conversion mechanics visible in the artefact**, which is what separates a case study from a gallery: the price qualifier in the hero lets unqualified traffic self-select out before filling a form; a callback form sits above the fold with a UAE-formatted phone field; a free brochure download acts as a lower-commitment second entry point further down. This is observation of the work, not invented outcome — no metrics were fabricated.
    - **Noted a deliberate design decision worth surfacing**: the Hadley creative uses a light editorial template distinct from the darker LEOS brand system, so development campaigns read as their own product without leaving the parent identity. Two visual systems, one identity.
    - **Schema**: added `mentions` with an `ApartmentComplex` entity for Hadley Heights (name, description, `numberOfAccommodationUnits: 216`, JVC/Dubai `PostalAddress`), so search engines and AI systems can resolve the development as a real place rather than as loose page text. `CreativeWork` and `BreadcrumbList` from item 45 unchanged. All three blocks confirmed to parse.
    - Title updated to "LEOS Developments & Hadley Heights — Real Estate Case Study" (63 chars, inside the SERP limit). `llms.txt` gained a second entry for the project with its own `#hadley-heights` anchor URL. Section carries `scroll-mt-28` so anchor links do not land under the fixed nav.
    - **Compression note for future uploads**: Bilal's Hadley landing page capture is **372 KB at 1920×6682**, versus the LEOS one at **3.3 MB at 1928×6555** — near-identical dimensions, roughly 9× the weight. His export settings improved between the two. Re-exporting the LEOS capture the same way is a free win and is the single largest asset on the site.
    - **Open UX issue observed, not changed**: the WhatsApp assistant auto-opens on this page and covers the fact strip on desktop. On a page whose whole job is to read as considered and professional, an auto-opening chat panel over the content works against that. Suggest suppressing auto-open on `/portfolio/**` and leaving the launcher available. Not actioned without Bilal's sign-off since the auto-open behaviour was an explicit earlier request (item 15).
    - Verified: `tsc --noEmit` and `npm run build` clean at 18 pages, 1 `<h1>` / 5 `<h2>` / 5 `<h3>`, 18 images with **0** broken, 12 figcaptions, zero console errors, no horizontal overflow at 390px, and the key facts (216, AED 1 million, Jumeirah Village Circle) confirmed present as text in the rendered HTML.

47. **Case studies restructured to data-driven client hub + project pages, and a duplicate-key bug fixed (2026-08-26)** — **done.** Bilal reported a React duplicate-key warning and clarified the hierarchy he wants: LEOS is the developer, Hadley Heights is one of their projects, projects should surface as **cards**, and Weybridge Gardens and Cavendish Square are coming next in the same shape.
    - **The key warning was a genuine pre-existing bug, not from the item-44 logo-wall change.** `LogoWall.tsx`'s placeholder array has held two entries with the identical label `[Client Logo — Real Estate Agency]` since it was written, and the list used `key={logo.name}`. React therefore saw two children with the same key. Fixed by giving each entry an explicit `id` and keying on that, rather than by renaming the placeholders (two clients can legitimately share a display label, so the label was never a safe key).
    - **Promoted projects from sections to their own routes**, which reverses the "default is a section" call made earlier the same day. The reason changed: that call assumed projects had thin material. Hadley Heights arrived with its own logo, a 1920×6682 landing page capture and a 5-slide carousel, and two more projects are queued. Three such projects inline would have made the client page enormous, and each development deserves to target its own name. Client pages still consolidate the "several launches for one developer" story, which was the original point.
    - **New structure**, all generated from one file: `/portfolio/[client]` (hub: hero, project cards, scope, client-level website + brand social) and `/portfolio/[client]/[project]` (detail: hero, fact strip, landing page, campaign gallery, sibling-project links). URLs preserved — `/portfolio/leos-developments` still resolves, so nothing already indexed breaks.
    - **`src/data/caseStudies.ts` is the single source of truth**, following the `pillars.ts` pattern that has repeatedly prevented drift in this project. A `Client` owns `Project[]`; each carries facts, captures, galleries with per-image alt/caption, and optional `place` data for schema. **Adding Weybridge Gardens or Cavendish Square is now a data-only change** — routes, cards, sitemap entries, breadcrumbs and structured data all generate from it. `sitemap.ts` imports `caseStudyUrls()` so no sitemap edit is needed either.
    - Extracted `CaseStudyParts.tsx` (`CaptureFrame`, `GalleryGrid`, `FactStrip`) so the hub and detail pages share one implementation rather than duplicating the browser-chrome frame and captioned grid.
    - **Structured data per level**: the hub emits `CreativeWork` with `hasPart` listing each project, the detail page emits `CreativeWork` with `isPartOf` pointing back plus the `ApartmentComplex` entity, and both emit a correct-depth `BreadcrumbList` (4 levels on the detail page). Confirmed all parse in the rendered HTML.
    - Verified: `tsc --noEmit` clean (after the stale `.next/types` validator entry for the deleted route cleared on rebuild), `npm run build` clean at **19 pages** with both dynamic routes statically prerendered, all 4 URLs returning expected codes including a correct **404** for an unknown project slug, sitemap emitting all 3 portfolio URLs, project page title at 54 chars, 1 `<h1>`, 5 figcaptions, zero console errors, and **no duplicate-key warning remaining** on the homepage.

48. **Refine and Tomorrow World logos added; logo wall is now all real marks (2026-08-26)** — **done.** Bilal supplied `public/portfolio/refine/logo/refine-dubai-logo.svg` and `public/portfolio/tomorrow/logo/tomorrow-world-logo.svg`.
    - **Rendered both on dark, surface and white backgrounds before wiring them in**, rather than trusting the filenames — the same check that caught the misleading "dark"/"light" naming in item 35. Both are correct for this site: Refine is a white vector wordmark in brackets, Tomorrow World is a gold chevron mark over white type. Both are invisible on white, which is expected and fine here since the site is dark-only.
    - **Finding: the Tomorrow World file is not a real vector.** It is a **612×372 raster image base64-embedded inside an SVG wrapper**, with a drop-shadow filter, at a 2004×1220 viewBox. It renders cleanly at logo-wall size (92×56 measured) but will soften noticeably if ever used larger — a case study hero, for instance. Worth requesting a true vector from Tomorrow World's design team before it is used at scale. Refine's file is genuine vector.
    - **Bracket placeholders removed.** The wall previously carried 1 real logo plus 4 `[Client Logo — …]` text tiles. Showing brackets to a prospect reads as unfinished, so with three real marks available the placeholders were dropped and the grid narrowed from 5 columns to 3. **Flagged for Bilal**: this is a judgement call about how the site looks, easily reverted, and more logos (Mövenpick) can simply be appended.
    - **Only LEOS links.** `Logo.href` is now optional: a logo with no `href` still renders as a real mark but is not clickable. Refine and Tomorrow World have no case study assets yet, and linking them to an empty page would be worse than not linking. They become clickable the moment their assets land.
    - Added per-logo `heightClass` because optical weight differs by lockup shape: a stacked mark (Tomorrow World, 1.6:1) needs more height than a wide wordmark (LEOS, 3.6:1) to read as the same size. Measured result: 116×32, 92×56, 123×40 — visually balanced rather than mathematically equal.
    - Section copy updated: the description still said "Logos go here once approved by each client," which was stale, and the highlight moved from "Real Estate, SaaS & Startups" to "UK & UAE Real Estate" to match what the wall actually shows.
    - Verified at 1440px and 390px: 3 logos, **0** broken, **0** placeholder strings remaining, no duplicate-key warnings, no horizontal overflow. `npm run build` clean at 19 pages. The 1 remaining lint error in this file is the pre-existing `<a>`-vs-`<Link>` issue tracked as Phase 0 item 3, not new.

49. **Fixed a sitewide empty-band bug, reordered the case study, and squared the galleries (2026-08-26)** — **done.** Bilal screenshotted a black gap between the nav and the content on the LEOS page and asked for three changes.
    - **The empty band was a real layout bug affecting 5 pages, not just the one he saw.** Every inner page put its top padding on `<main>` (`pt-32 sm:pt-40`) while the ambient `.grid-fade` background lived on the hero `<section>` inside it. The grid therefore started ~160px down the page, leaving a plain black strip under the fixed nav and a visible seam where the grid abruptly began. Fixed by moving the padding off `<main>` and onto the hero section itself, so the grid renders from the very top and continues behind the nav. Confirmed by measurement: the `.grid-fade` element's `top` is now **0** rather than positive. Applied to `/about`, `/contact`, `/services`, `/portfolio/[client]` and `/portfolio/[client]/[project]`. `/services/[slug]` was checked and left alone — it opens with a bare `<nav>` rather than a grid-fade hero, so it has nothing to bleed through.
    - **Development campaign cards moved to the bottom**, after the corporate website and brand social sections, per Bilal's instruction. Final order: hero → scope of work → corporate website → brand social → development campaigns → CTA. Given the cards now close the page rather than sit mid-flow, the section gained a top border and extra padding so it reads as a deliberate final block instead of a stray row.
    - **Galleries are now 4 per row with equal tiles.** Grid went from `lg:grid-cols-3` to `lg:grid-cols-4`, `sizes` updated to `25vw` to match so `next/image` keeps serving correctly-scaled files. The equal-size part needed more than a column change: the LEOS brand assets mix 1:1 (1080×1080, 1200×1200) with 5:4 (1349×1079, 1350×1080), so rows previously came out ragged. Images now use `aspect-square object-cover`. Verified all 7 tiles render at exactly **291×291**.
    - **Trade-off worth knowing**: `object-cover` centre-crops the two 5:4 assets, which trims a little from the sides — the Weybridge Gardens corner logo is slightly clipped as a result. The alternative is `object-contain`, which crops nothing but letterboxes those two tiles against the card background. Chose cropping since Bilal asked for equal sizes and the subjects are centred, but it is a one-line switch if he prefers the full asset visible.
    - **A mistake worth recording**: the first attempt placed an explanatory JSX comment directly after `return (`, before the root element, which is a syntax error — a JSX expression cannot open with a comment. Moved it above the `return` as a normal JS comment. Caught by `tsc`, not shipped.
    - Verified on the production build: `tsc --noEmit` clean, `npm run build` clean at 19 pages, section order confirmed programmatically, 7 gallery tiles at 4 per row all identical in size, `.grid-fade` covering the page top, zero console errors.

50. **Contact form built, and the lead API hardened so submissions can no longer be silently lost (2026-08-26)** — **done.** Bilal asked for a form on `/contact` that posts leads to his other app. The form was deliberately left off this page when it was built (item 42) precisely because `/api/lead` returned 503 with no CRM configured and would have accepted enquiries and dropped them, so that defect (Phase 0 item 1) was fixed in the same pass rather than shipping a form on top of it.
    - **`ContactForm.tsx`**: name, email, phone, service (options generated from `pillars.ts` so they cannot drift from the services pages), and a project description. Every field has a real `<label>`, status changes are announced via `aria-live`, and the submit button has a distinct submitting state.
    - **The important behaviour is what happens on failure.** If the lead cannot be delivered, the form does **not** show a dead error. It renders a fallback panel with WhatsApp and email buttons **prefilled with everything the visitor typed**, so a genuine enquiry still reaches Bilal. This is the difference between a broken form and a form with a safety net, and it matters most right now because no endpoint is configured yet.
    - **API route rewritten**: email format validation, a best-effort in-process rate limit (5 per 10 minutes per IP, with an honest comment that per-instance state does not guarantee a global cap on serverless), a 10-second timeout on the outbound call, and full payload logging under an explicit `[LEAD NOT DELIVERED]` marker on every failure path so a lost lead is at least recoverable from logs. Responses now carry a `fallback` flag telling the client whether to offer the direct-contact route.
    - **Proved the happy path against a mock endpoint rather than assuming it.** Stood up a local server standing in for Bilal's app and confirmed it receives `{name, email, phone, message, service, meetingTime, source, receivedAt}` as JSON with `Authorization: Bearer <key>`, and that `source` correctly identifies the originating form (`contact-page-form`). Also verified: bad email → 400, missing fields → 400, honeypot → silent 200, 6th rapid post → 429, endpoint down → 502 with fallback, endpoint unset → 503 with fallback.
    - **Two of my own measurement mistakes, both caught and corrected before reporting**, and both the same class as items 43 and 45: (1) a honeypot "visible?" check returned true, but Playwright counts off-screen elements as visible since they are not `display:none` — the correct check is the bounding box, which confirmed it sits at `left: -9894px` and outside the tab order; (2) a check for prefilled fallback links returned false because `querySelector('a[href*="wa.me"]')` matched the *channel card* higher up the page, not the fallback panel. Scoping to the panel confirmed both WhatsApp and mailto carry every field. Lesson repeated: scope selectors to the element under test, and pick a check that matches the property actually being asserted.
    - **Redundancy removed**: the numbered "what to include in your first message" card was dropped, since the form now asks those questions directly as fields.
    - **`.env.local.example` documents the exact payload contract** — field names, the `source` values for all three capture paths, and a note that field mapping can be adjusted in the route if Bilal's app expects different names. The test `.env.local` pointing at the mock was removed so nothing bogus is left behind.
    - **Still needed from Bilal**: the endpoint URL for his other app, and an auth token if it needs one. Once in `.env.local`, no code changes are required unless the field names differ.
    - Verified: `tsc --noEmit` clean, `npm run build` clean at 19 pages, all 5 fields correctly labelled, submission succeeded end to end into the mock with the success panel rendering, fallback panel confirmed with fully prefilled links, zero console errors.

51. **Campaign attribution unified across all three lead paths, plus a consent banner and a privacy page (2026-08-27)** — **done.** Bilal asked for the chat and form submissions to reach Performo with UTM data attached, and for a cookie option.
    - **Three different UTM shapes existed before this.** `WhatsAppButton.tsx` had its own `utmFields()` sending camelCase `utmSource`; `ContactForm.tsx` had a near-identical copy; `LeadFormPopup.tsx` sent **no** attribution at all. Sending two shapes and a gap to one intake endpoint would have made attribution unreliable at exactly the point it matters. All three now call a single `getAttribution()` from `src/lib/attribution.ts`.
    - **First-touch attribution now survives the visit.** The old helpers read `window.location.search` at the moment of submission, so a visitor who landed on an ad URL and then browsed to `/contact` arrived with nothing — the common case, and the one that made attribution near-useless. Attribution is now captured on the landing page and persisted for the session. **Verified end to end**: landed on `/?utm_source=google&utm_medium=cpc&utm_campaign=offplan-dubai`, navigated to `/services`, submitted from `/contact`, and Performo received the original campaign. Same test passed via the WhatsApp chat from `/portfolio` with a Meta campaign.
    - **Consent gates it, so the banner is not decorative.** `CookieConsent.tsx` asks once and stores the answer in local storage (storing the answer itself is necessary-category, so it is not gated). Accept → attribution persists to session storage. Decline → **nothing is stored**, and a submission still carries any params present on the URL at that moment, read live and never written, so a single-page visit straight from an ad is not lost. Both paths tested: accept sent the campaign, decline sent none.
    - **`/privacy` built** because a consent banner linking nowhere is incomplete. It is deliberately a plain factual inventory — what is stored, where, why, and whether it is optional — rather than boilerplate legalese, and it states plainly that the site runs no advertising cookies, third-party trackers, or analytics scripts (true today; revisit when Phase 1 lands GA4). **Flagged: this is a factual description, not legal review.** Whether it satisfies UAE PDPL or GDPR for his client base is a question for a qualified professional, and this closes suggestion-queue item 6 only in the technical sense.
    - **Real limitation worth acting on: Performo has no fields for click IDs.** The intake mapping documents that `gclid`, `fbclid`, `ttclid` and `msclkid` are collected by the site but dropped at the boundary. This matters more than it sounds for a performance marketer: **Google Ads offline conversion import is keyed on `gclid`**, so without storing it there is no way to feed "this lead became qualified" back to Google and let Smart Bidding optimise toward real pipeline rather than raw form fills. Same applies to Meta's CAPI with `fbclid`. Adding those columns to Performo's Lead model is the single highest-value change on the receiving end, and it connects directly to the server-side tracking service in Phase 3 item 15.
    - Also verified: Performo receives `type: "chat"` vs `"form"` correctly per source, auth goes as `x-api-key`, and the fallback behaviour from item 50 still holds when the endpoint is unreachable.
    - `/privacy` added to the sitemap and linked from the footer. Test `.env.local` removed.
    - Verified: `tsc --noEmit` clean, `npm run build` clean at **20 pages**, banner shows once and not again after answering, zero console errors.

52. **Performo wired to the real endpoint; concurrent-session clobbering flagged (2026-08-27)** — **done, with a caveat.**
    - **A second Claude session was editing this repo at the same time**, and the two of us overwrote each other. Specifically: while testing item 51 I wrote `.env.local` twice pointing at a local mock (`http://localhost:4555`) and then deleted it, destroying the real Performo credentials that session had put there. **That was my error** — the file already existed and I neither checked nor backed it up before writing. `.env.local` is gitignored, so there was no version history to recover from either.
    - **Rule going forward: never write `.env.local` without checking whether it already exists.** For local testing, override inline for a single command (`CRM_LEAD_API_URL=… npm run dev`) or use a separate file, rather than replacing the developer's real configuration.
    - **Real credentials restored.** `.env.local` now points at Performo's production intake with the `x-api-key` value; the URL and key live only in that file and are deliberately **not** recorded here, since this file is committed. Verified the env loads without creating junk data in the production CRM: an intentionally invalid email returns **400** (rejected at validation, before any outbound call) rather than the **503** that an unconfigured endpoint would give. A live round-trip test was deliberately **not** run, because the other session had already verified both the form and chat paths against the real backend and cleaned up its test leads — repeating it would only add more junk to Bilal's real pipeline.
    - **The integration contract, confirmed by that session against the live backend**: endpoint takes `x-api-key` (not `Authorization: Bearer`, which was the original mismatch); `type` is `"chat"` when `source` contains "whatsapp" and `"form"` otherwise; `name`, `email`, `phone`, `message`, `service` pass straight through; UTM data prefers the nested `attribution` object from `getAttribution()` and falls back to flat `utmSource`/`utmMedium` fields.
    - **Still open from item 51**: Performo's Lead model has no columns for `gclid`/`fbclid`/`ttclid`/`msclkid`, so click IDs are collected by the site and dropped at the boundary. This blocks Google Ads offline conversion import, which is keyed on `gclid`.

53. **Live site found to be rejecting every lead — deployed build is stale (2026-08-27)** — **diagnosed, fix is a redeploy.** Bilal added the Performo env vars on Hostinger. Checking that surfaced a live production problem.
    - **`bilalshafqat.com` is now serving the Next.js app, not WordPress.** Confirmed via `x-powered-by: Next.js` behind LiteSpeed. This supersedes earlier notes in this file saying the app was unshipped — it is live, and `/`, `/about`, `/contact`, `/services`, `/portfolio` and both case study routes all return 200. The API route runs, so Hostinger is giving it a real Node runtime.
    - **The live lead pipeline is broken.** A valid payload POSTed to `https://bilalshafqat.com/api/lead` returns **502 "The CRM rejected this submission"**, while the identical payload against local code with the same credentials returns **200**. The credentials are therefore correct and the integration works — **the deployed build is the problem**, almost certainly still sending `Authorization: Bearer` instead of Performo's `x-api-key`.
    - **Consequence, and it is costing real leads**: the lead popup and the WhatsApp chat both post to this route, so every enquiry submitted on the live site is currently being rejected. The item-50 fallback panel (which rescues a failed submission via prefilled WhatsApp/email) is **not** deployed either, so visitors just see a generic error and the lead is gone. This has been the state since whenever the current build shipped.
    - **Deployed build dated by feature probes**: logo wall with real logos (item 48) and the GSAP mega menu (item 43) are live; the contact form (item 50) and cookie banner (item 51) are not. So production is roughly one day behind local, deployed on 26 Aug after item 48.
    - **Fix: redeploy the current `main` build.** No code change is needed — everything required is already local and verified. `/privacy` (404 in production) lands with the same deploy.
    - **Test data to clean up**: verifying this required real POSTs to production Performo. Two were rejected and created nothing. One succeeded against the local build and **did create a real lead**, named `ZZ TEST - DELETE ME (local check)`. Bilal should delete it.
    - **Also observed**: old WordPress URLs (`/training/`, `/about-me/`) now 308-redirect into 404s, and `/wp-json/` is gone. Expected, since the migration and its redirect rules were reverted on 17 Aug, but it confirms the ~81 legacy URLs are dead in production and their old rankings will decay. Revisit if any of them still earn traffic in Search Console.

Reference sites (adapt style, do not copy content):
- https://www.brionycullin.com/ (low-friction consultation CTA)
- https://www.punith.com/ (process steps)
- https://muradmurad.com/ (industries worked in — style reference only; not used as a separate homepage section, see resolved decisions)
- https://www.ryrob.com/ (growth story section — currently folded into About Me, could be pulled back out as its own section if it gets lost)
- https://www.sajidsulaiman.com/ (detailed breakdown section — currently folded into About Me)
- https://growwithward.com/ ("how I grow your business" framing — currently expressed via Growth Partnerships/Engagement Models instead)

## UX target

- Core Web Vitals (2026 Google real-user thresholds): LCP under 2.0s, INP under 200ms, CLS under 0.1
- WCAG AA accessibility: contrast, alt text on every image/logo, full keyboard navigation
- A real photo or short video of Bilal near the hero/process section (strongest personal trust signal for a solo freelancer) — **done**, keep it in the hero
- On mobile, collapse the deepest sections (growth story, detailed breakdown, "how I grow your business") or move that depth to inner pages so the homepage stays skimmable
- One consistent CTA style repeated throughout, never introduce a second competing CTA style
- Real user testing (heatmaps/session recordings) after launch — this is what actually closes the gap to excellent UX, not layout alone

## SEO target (Google, Bing — Yahoo results are Bing-powered so this covers both)

- HTTPS, XML sitemap submitted to Google Search Console and Bing Webmaster Tools
- Canonical URLs, unique meta title/description per page
- JSON-LD structured data: Organization, Service, FAQPage, Review schema
- Alt text on all images including the logo wall
- Internal linking across homepage, services, portfolio, blog
- E-E-A-T content signals: real case studies, credentials, named testimonials, blog content answering real client search queries
- Backlinks from credible sites (slow-moving factor, needs ongoing outreach, not a one-time build task)

## AI search / GEO target (ChatGPT, Claude, Perplexity, Gemini, Copilot)

This is an evolving space with no official published ranking algorithm from these providers as of 2026. Revisit every few months.

- robots.txt: explicitly allow GPTBot, ClaudeBot, Google-Extended, PerplexityBot; decide CCBot case by case
- Add an llms.txt file describing site content and sections. Note: as of early 2026, roughly 90% of AI crawler traffic ignores llms.txt and reads HTML directly, so clean semantic HTML matters more than the file itself
- Structure key content in question-first, FAQ-style blocks that match how clients actually phrase search prompts
- Use specific numbers in case studies (e.g. "3.2x ROAS") since AI engines favor evidence-dense, extractable content over vague claims
- Keep content refreshed; AI citation pools favor recently updated pages over stale ones

## Important caveat to keep repeating to Bilal

No on-page work guarantees a top ranking on Google, Bing, Yahoo, or citation in AI search results. Backlinks, domain age, and competition all matter and take months to build. This file describes what's fully controllable through design and technical build quality, not a ranking guarantee.